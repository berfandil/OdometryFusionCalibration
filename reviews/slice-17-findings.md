# Slice 17 review — turn-regime full rotation-extrinsic (rot3d) @ f1eee59

VERDICT: APPROVE WITH FIXES — the rot3d core (math, convention, observability spine, persistence, strict core, default-off) is correct and well-pinned; the two MAJORs are the implementer's own open items 1 and 2, which are real functional gaps against the spec's stated wins, not latent corruption. Counts: 0 BLOCKER, 2 MAJOR, 3 MINOR, 2 NIT.
Verified independently: math — Kabsch det-correction `U·diag(1,1,sign det(UVᵀ))·Vᵀ` is the standard solution with the sign on the smallest singular direction (JacobiSVD sorts descending, column 2); the λ_mid gate reads `ev(1)` of Eigen's ASCENDING SelfAdjointEigenSolver output, which IS the middle eigenvalue, with a `ev(2)>0` zero-information guard; Mw and BBw are decayed by the same γ on the same line as their deposit (never one without the other); δφ = log(R̂·R_bpᵀ) with readout exp(φ_mode)·R_bp is a consistent left-residual pair, and the re-anchor is contractive (R̂ is Mw-driven, basepoint-independent — pinned by the calibrator-level re-anchor test, including BBw surviving the reset). Convention — a = X.R·b with X = sensor→base matches B = X⁻¹∘A∘X (log(R_A) = R_X·log(R_B)); the sign IS pinned, not just the magnitude (test asserts `yaw_rec > 0.25` for a planted +0.3, plus the exact `a = X.R·b < 1e-12` pin against make_extrinsic = Rz·Ry·Rx, the parse_extrinsic/inject_calib convention). Wiring — publish order inside apply_calib_feedback is ext/roll (estimator.cpp:658) THEN rot3d (estimator.cpp:682), and nothing later writes prior_extrinsic[i].R, so a committed rot3d cannot be clobbered; the rising-edge re-anchor (estimator.cpp:690) runs AFTER the publish, so it anchors onto the just-published committed value; the rot3d_bp_-vs-prior_ split is real and HANDLED — set_basepoint's every-step value sync deliberately does not move rot3d_bp_ (calibration.cpp:653 writes prior_ only), rot3d_bp_ moves only at registration (set_prior, called solely from add_source at estimator.cpp:903), at the rising edge, and at restore, each paired with reset_rot3d, so the accumulated δφ never changes meaning under live votes; reference exclusion pinned at estimator.cpp:590. Persistence — v2 bump complete (serialize writes the flag, deserialize reads it, test pins kFormatVersion==2 and v1-stamped blob → VersionMismatch); config-hash covers the flag (flip-rejects, tested); restore re-anchors the rot3d basepoint onto the restored prior_extrinsic.R + resets channels; the restored flag holds through refill via commit_gate_reanchor's `votes < min → stay committed` guard — the same mechanism ext/roll/lever/scale use, so a dedicated offset_restored-style mirror is NOT needed (offset's flag exists for its different gate machinery): acceptable. Strict core — fixed arrays (Mat3/Histogram1D × kMaxSources), fixed-size Eigen 3×3 JacobiSVD + SelfAdjointEigenSolver (no heap), all loops bounded. Default-off — the only off-path code change is `so3::log(R_A)` captured as a Vec3 before `.norm()` (numerically identical); the rot3d block is guarded at calibration.cpp:827 with zero accumulator/histogram work when off, estimator calls are flag-gated, and the planar exact-equality run pins flag-on==flag-off bit-identically (also the BBw-gate mutation guard). Trust apparatus — median/ESKF/Phase-1/best_roll/cov-cal diffs are zero; the roll/xyz vote path in observe() is untouched.

src/core/calibration.cpp:798: MAJOR: (implementer open item 1 — confirmed real) the lever/translation row still builds R_X = R_yp·Rx(roll) even after rot3d commits; on the slice's target regime (turn-only 3D, Phase-1 starved) R_yp stays frozen at the wrong prior basepoint, so the ~|t|·θ lever bias the spec §2 "lever coupling win" promises to remove persists, and the real-data acceptance "lever re-measured, expect ≪ 3–4 cm" (§3) cannot be met by this commit. fix: when rot3d_enabled_ and the slot's rot3d channels are non-empty (or a committed signal is fed down), use the rot3d readout as the translation row's R_X (b = rot3d(id)·t_B − t_A); reset_lever on the rot3d rising edge since prior rows were built with the stale R_X.
src/core/estimator.cpp:552: MAJOR: (implementer open item 2 — confirmed real) participates() under ColdStart::ReferenceOnly — the DEFAULT cold_start — keys on ext_committed only, so on a never-straight rig (exactly the EuRoC/drone regime this slice exists for) a rot3d-committed source has a fully committed rotation yet NEVER joins the fusion median: the slice's win cannot reach fusion under the default cold-start. fix: `return ext_committed[i] || rot3d_committed[i];` (rot3d commit is a strictly stronger rotation guarantee than ext commit) + a ReferenceOnly turn-only rig test pinning the source joins after the rot3d commit.
src/core/estimator.cpp:690: MINOR: (implementer open item 3 — confirmed a gap, low risk) the estimator-level rising-edge re-anchor is not mutation-pinned: the bootstrap test's 0.41 rad prior error fits inside the ±0.8 so3_hist range, so deleting the set_rot3d_basepoint/reset_rot3d pair would likely still pass every test (votes land in-range, mode reads the full residual, readout still ≈ truth) — the re-anchor is load-bearing only for prior errors beyond the histogram range, where votes clamp to the edge bin and convergence needs the contractive walk. fix: add a bootstrap test with prior rotation error > so3_hist range (e.g. ~1.2 rad) that fails without the re-anchor (mirrors the Slice-8 large-error ext walk).
src/core/estimator.cpp:1326: MINOR: CalibSnapshot::extrinsic_committed stays ext_committed even when the snapshot's rotation is rot3d-driven — on a turn-only rig the snapshot publishes a fully committed rotation while reporting extrinsic_committed=false, so any consumer keying on the flag (harness filtering/printing, downstream gating) misreads the published R as uncommitted. fix: either OR rot3d_committed into the flag (it is also monotone-per-regime, so no oscillation) or document in result.hpp that the rot3d commit is surfaced only via Estimator::rot3d_committed().
tests/test_calib_rot3d.cpp:418: MINOR: no MIXED straight+multi-axis-turn test where Phase-1 ext/roll AND rot3d are committed simultaneously — the precedence claim ("supersedes, no thrash") is pinned only on regimes where one side starves; the every-step ext/roll publish + rot3d override + calib2.set_basepoint sync loop is plausible-stable by inspection but unpinned, and the rot3d-DROP fallback (published R reverting to the roll-committed R_yp∘Rx(roll) composition, a possibly worse rotation) is untested. fix: add a mixed-trajectory rig test asserting both commits latch, the published R stays the rot3d value across steps (no oscillation), and fused error stays bounded.
src/core/calibration.cpp:831: NIT: Mw/BBw decay is skipped (together with the deposit) when ‖b‖ < kRotRowMin — a literal reading of spec §2 "decay-aged per accepted window" would decay on every gate-accepted window; the implementation decays only on rot3d-accumulated windows. Self-consistent (decay never runs without a deposit, so the effective window count matches) and arguably better, but it deviates from the spec sentence. fix: one comment line documenting "accepted = rot3d-row-accepted" (or note it in SLICE17 §2).
tests/test_calib_rot3d.cpp:83: NIT: every rot3d test forces VoteWeight::One; no test runs rot3d under Combo (the shipped real-data weighting) — the λ_mid/λ_max gate is weight-ratio-invariant and the commit_min_votes mass semantics are already documented for the other DOFs, so this is consistency, not correctness. fix: one Combo-weighted multi-axis convergence case (loose bound) to pin that the gate and commit still behave under mass-weighted votes.
