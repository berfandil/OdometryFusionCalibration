# Slice 7 review — Phase-2 calibration (roll + xyz hand-eye, both strategies) @ 28b3fb1

Scope: `calibration.{hpp,cpp}` (Phase-2 additions), `estimator.cpp`/`result.hpp` diffs, `test_calib_phase2.cpp`, against DESIGN §3/§6, DECISIONS D5/D10/D21, CONFIG §6/§7/§8, `histogram.hpp`, `lie.{hpp,cpp}`, oracle `synthetic_source.cpp`. Built + ran: full suite green; the 10 phase2 cases pass (107 assertions). Math verified self-consistent: oracle `B=X⁻¹AX` ⇒ `AX=XB`; translation `(R_A−I)t_X = R_X t_B − t_A` (derived `R_X t_B + t_X = R_A t_X + t_A` ⇒ exact, no sign/transpose error); rotation residual `‖log(R_X R_B R_Xᵀ R_Aᵀ)‖=0` at truth; `R_X(roll)=R_yp·Rx(roll)` matches the planted `Rz·Ry·Rx(roll)` when fed `R_yp=Rz·Ry`. `best_roll` grid `r(k)=−π+step·(k+1)` covers (−π,π] in kRollGrid samples; parabola neighbours `r(best_k−1)`/`r(best_k+1)` correct, wrap-safe via 2π-periodicity of rot_residual. yaw-only z-null and the LDLT/eigen guards verified. normal-equation form correct.

src/core/calibration.cpp:548-573 / config.hpp:96: MAJOR - `vote_weight` (CONFIG §7, default `Combo`, D5 "votes weighted by ‖ω‖") is never read; every roll/xyz vote is hardcoded weight 1 (Phase 1 ignores it too). The documented turn-magnitude/confidence weighting is silently a no-op. Honor `cfg.vote_weight` (capture in configure(), scale the add() weight by ‖fused_ω‖ and/or confidence) or remove the knob from CONFIG and document `one` as the only mode.
src/core/calibration.cpp:388-405,615-627: MINOR - the COMMITTED lever_arm() (histogram mode of solve_ridge's prior-centred votes) biases observable axes toward the prior by ~ridge/λ ≈ kVoteRidgeRel = 1e-3 of (t*−t_prior); diagnostic solve_lever_arm() is unregularized. The tight convergence tol (3e-2) is checked on solve_lever_arm, not on the committed lever_arm (loose 5e-2 with t_prior=0), so the committed path's ~0.1% prior-pull is untested. Acceptable magnitude; document, and consider tightening the lever_arm() assertion with a nonzero prior to catch a larger ridge.
tests/test_calib_phase2.cpp:488-533: MINOR - the e2e wiring test plants the FULL true rotation (incl. roll) into the source prior, so Phase 1 returns R_yp≈X.R (roll already baked in) and Phase 2 recovers roll≈0; the rotation assertion (`so3::log(Xᵀ·cs.R)<6e-2`) passes vacuously on the roll DOF (it never asserts roll≈roll_p). True roll recovery is exercised only by the unit convergence cases (which feed roll-free R_yp). Honest per the "Phase-2 presumes converged Phase-1" note, but add a roll-value check or a roll-free-prior wiring variant so a broken roll seam can't pass e2e.
include/ofc/core/calibration.hpp:336 / src/core/calibration.cpp:325-333: MINOR - kCondMin, kAxisInfoMin, kVoteRidgeRel, kRotRowMin, kRollGrid are hardcoded namespace constants, not config-exposed; the conditioning floor and ridge govern the rank-deficiency behaviour the slice is judged on. Acceptable (CONFIG lists no such knobs) but the load-bearing observability thresholds should be documented as fixed or surfaced for tuning.
src/core/calibration.cpp:599: MINOR - `lo < kCondMin * hi` uses a strict `<`; an exactly-at-floor eigenvalue (lo == kCondMin*hi) is accepted as observable. Boundary-only; deterministic. Use `<=` if the floor is meant to be exclusive, else document the inclusive boundary.
src/core/calibration.cpp:546,634 / estimator.cpp:457-460: NIT - R_yp falls back to prior_[slot].R (the FULL prior rotation incl. its prior roll) when ryp not set; if a source has a nonzero prior roll but no Phase-1 R_yp yet, the roll basepoint already contains roll and the recovered roll double-counts the prior roll until Phase 1 feeds R_yp. In the wired estimator R_yp is set every step so this never bites; flag for the bootstrap/Slice-8 path.
include/ofc/core/calibration.hpp:249: NIT - kRollGrid=180 over (−π,π] is a 2° pre-refine grid; a residual minimum narrower than ~2° between samples could let the grid pick the wrong neighbourhood before the parabola. Fine for the smooth single-well rot_residual here; note if multi-well cases (large yaw+roll coupling) ever arise.

CLEAN categories (verified, no finding):
Roll math + S¹ voting — R_X=R_yp·Rx(roll) seam consistent with Phase-1 extrinsic.R=δR·R_Xprior; residual is the correct hand-eye rotation residual (=0 at truth, flat only at R_A=I); bounded grid + parabolic refine + (−π,π] wrap singularity-free; voted into circular roll_hist correctly.
xyz linear-LS — normal equations AᵀA t=Aᵀb accumulated into a fixed 3×3 + 3×1 (noalias, no growing matrix); LDLT solve; b=R_X t_B−t_A sign/transpose correct; ridge centred on prior leaves the unobservable axis AT prior (dt≈0 when λ≈0) and resolves observable axes.
Rank-deficiency handling — SelfAdjointEigenSolver conditioning floor (lo≥kCondMin·hi) gates the full solve; per-axis info floor (AtA(c,c)≥kAxisInfoMin·max_diag) gates voting; pure-yaw z is the exact null space of Rz−I ⇒ z never votes ⇒ low translation_confidence. Sound.
Both strategies — VsFusedBase uses A=fused consensus; PairwisePinnedRef uses A=X_ref∘B_ref∘X_ref⁻¹ with X_ref held at prior (never updated) and the reference skipped from voting (its hand-eye vs A_impl is degenerate); ref-absent ⇒ NotReady. Both correct.
Turn gate + near-singular row — votes only when ‖fused_ω‖>turn_omega_min; per-window ‖log R_A‖<kRotRowMin rejected before accumulation (ω×r→0). Re-derives the gate. Sound.
Observability self-test (LOAD-BEARING) — straight()-only asserts voted==0, roll/xyz vote_count==0, roll==0@1e-9, solve_lever_arm obs==false + t==prior@1e-9, both confidences==0. Strong; a weak "runs" pass is impossible. Convergence pins roll@2e-2/3e-2 and t_X@3e-2 for BOTH strategies + full-rotation@3e-2. Yaw-only tested as partial observability (x/y recovered@5e-2, z obs==false + cz<0.2).
Estimator wiring — CalibSnapshot gains translation_confidence (non-breaking); extrinsic_confidence = min(yp,roll) once roll has votes else yp-only; cs.extrinsic=calib2.extrinsic (yp∘roll + lever arm); R_yp pushed from live Phase-1 each step; no feedback into fusion (Slice 8); fusion path unchanged.
Strict core / determinism — roll+xyz histograms + 3×3/3×1 accumulators configured at init; observe()/readouts heap-free, bounded by source_count_/3/kRollGrid; double math; Eigen fixed-size LDLT/eigensolver deterministic; SlidingK fixed-order; determinism test asserts exact equality across 6 outputs.

TOTAL: 7 findings (0 CRITICAL, 1 MAJOR, 4 MINOR, 2 NIT) + 7 clean categories.
