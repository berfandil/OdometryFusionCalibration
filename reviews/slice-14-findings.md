# Slice 14 review — NEES consistency + golden regression @ 3822def

Scope: read the full diff (`tests/test_validation.cpp` NEW, `tests/CMakeLists.txt`) + the math it relies on in context — `src/core/lie.cpp` (`se3::exp`/`log`/`adjoint`, `so3` log/jacobians), `src/core/eskf.cpp` (predict-only covariance propagation `F = Ad(delta⁻¹)`, init), `src/core/estimator.cpp:909-921` (the `eskf.init(SE3{}, Mat12::Identity())` + published `frontier`), `include/ofc/core/{types,eskf,lie}.hpp`, `sim/src/rig.cpp` + `sim/include/ofc_sim/rig.hpp` (`gt_frontier` anchoring, `pose_error`), `sim/src/synthetic_source.cpp` (noise path / noise-free path); against DESIGN §5/§7/§10, DECISIONS D21/D24, ISSUES "Slice 14" done-criteria, HANDOFF §5/§6. Built + ran full gate green: 165 cases / 17315 assertions, 0 failed; ctest 1/1 pass in 3.32s (cosmetic `vswhere.exe`/`LF→CRLF` lines ignored). The 4 new cases: 12 (convention) + 3 (Monte-Carlo NEES) + 11828 (determinism replay) + 34 (committed golden). Observed live: ensemble-mean pose NEES = **0.128893**, N=24180, chi² 99% interval on the mean [5.94277, 6.05754]; `truly_consistent=FALSE`; max tracking error (noise-free golden) < 1e-9. INDEPENDENT VERDICT: the "covariance is grossly PESSIMISTIC (NEES ≈ 0.13 ≪ 6)" finding is REAL and the error convention is CORRECT — the NEES math matches the tangent the covariance is actually propagated in (eskf.cpp `se3::adjoint` = full SE(3) Ad), the 46× gap is too large to be a convention/units artifact, and the arithmetic is order-of-magnitude consistent with the verified `P = I₁₂` init being ~100× the true error.

tests/test_validation.cpp:241-247,266: MINOR - the case TITLE and header claim the reconstruction identity `T_est ∘ exp(e) == T_gt` "validates the convention ALGEBRAICALLY", but with `e = se3::log(T_est⁻¹ ∘ T_gt)` this identity is a near-TAUTOLOGY of `se3::exp`/`se3::log` being mutual inverses (lie.cpp:97-113 — `exp` uses `left_jacobian`, `log` uses `inv_left_jacobian`), reducing to `T_est ∘ (T_est⁻¹ ∘ T_gt) == T_gt` for ANY pose pair. It proves `e` is `se3::log`'s OWN right-tangent (and, with :251-258, that it is right- not left-error and `[trans;rot]`-ordered), but it does NOT execute any check that the COVARIANCE lives in that same tangent — that link rests entirely on eskf.cpp using the FULL SE(3) `se3::adjoint` (lie.cpp:115-122, the `[t]ₓR` coupling block) as `F`, which the header asserts in prose but no assertion tests. The conclusion is nonetheless correct (I verified eskf.cpp's Ad independently), but reword the header/title to "proves `e` is `se3::log`'s right-error tangent (right-not-left, trans-first); the covariance lives in this SAME tangent by eskf.cpp's full-SE(3)-Ad propagation (by inspection, not asserted here)" rather than implying the executable checks pin the covariance convention.

tests/test_validation.cpp:366-369: NIT - the diagnostic MESSAGE prints `truly_consistent=00007FF6856D8A18` (a code/pointer address) instead of `NO`. Observed live. The ternary `(truly_consistent ? "YES" : "NO")` chained into doctest's `MESSAGE(... << ...)` stringifier mis-binds the `const char*` (doctest captures it as a pointer, not a string). Cosmetic only — the actual verdict assertion `CHECK_FALSE(truly_consistent)` (:376) is correct and passes; only the human-readable line is garbled. Fix: build the verdict into a `std::string` / `bool`-to-`int` before the `<<`, or print `(int)truly_consistent`.

tests/test_validation.cpp:269-385: MINOR - the chi² consistency interval treats the N=24180 steady-state samples as INDEPENDENT, but they are autocorrelated across ticks within each of the M=30 runs (consecutive fused frontiers share overlapping windows and the same slowly-evolving P), so the TRUE effective sample count is much smaller and the real interval is WIDER than the printed [5.943, 6.058]. The header/comment do not acknowledge this. It does NOT change the verdict (even a 100× reduction in effective N widens the band only to ~[5.4, 6.6]; the observed 0.129 is still ~40× below), but the printed interval overstates its own tightness. Add a one-line caveat that the interval assumes independence and is therefore narrower than the autocorrelation-corrected band; the 46× gap survives either way.

tests/test_validation.cpp:500-526: NIT - the byte-identical determinism loop emits 11828 of the suite's 17315 assertions (the +11998 jump is almost entirely this one case): ~16 `CHECK`s per fused record × hundreds of records, all asserting the SAME property (deterministic replay) redundantly. Not incorrect and not slow (3.32s total), but it inflates the assertion count ~3.4× with non-discriminating repeats and dilutes the count as a signal. Consider hashing the (pose+cov+calib) bytes per record and comparing one digest per record (or per run), or sampling a fixed stride of records, to keep the assertion budget proportionate to the property tested.

DESIGN.md:85 / DECISIONS.md:102 / HANDOFF.md:83: MINOR - DOC DRIFT (note, do not fix): DESIGN §5, D21, and HANDOFF §5 all describe the pose error-state as "decoupled `SO(3)×ℝ³`", but the SHIPPED covariance is propagated by `F = Ad(delta⁻¹)` using the FULL SE(3) adjoint (`se3::adjoint`, lie.cpp:115-122, with the `[t]ₓR` translation-rotation coupling) — i.e. the covariance actually lives in the FULL coupled SE(3) tangent, not a decoupled split. The test header (test_validation.cpp:20-37) correctly states this and is RIGHT to contradict the design prose. This is a pre-existing doc-vs-code inconsistency (Slice 2, not introduced here) the orchestrator should reconcile: either the docs' "decoupled" wording or the impl's full-Ad propagation. The test's chosen convention (full `se3::log`) matches the CODE, which is the correct choice.

CLEAN categories (verified, no finding):
NEES convention correctness (TOP PRIORITY) — VERIFIED. The covariance is propagated as a right-invariant error `eta` with `T_true = T ∘ exp(eta)` and `F = se3::adjoint(se3::inverse(delta))` (eskf.cpp:7-13,64-80). `se3::adjoint` is the FULL SE(3) adjoint (lie.cpp:115-122), whose transport is correct precisely for the full SE(3) tangent in which the translation error is the `V⁻¹`-mapped `rho` of `se3::log` (lie.cpp:106-113). The test forms `e = se3::log(T_est⁻¹ ∘ T_gt)` (test_validation.cpp:126-127) — the SAME full-SE(3) right tangent, `[trans;rot]` index-aligned with `cov.block<6,6>(0,0)` (types.hpp:19,45; eskf ordering 0..5 pose). Right (not left) error confirmed by :251-253; rotation rows = `so3::log(R_estᵀR_gt)` (not transposed) by :256-258; trans rows = SE(3)-log residual (not raw `t_gt−t_est`) by :263-264. The convention is correct and matches the code (the brief's feared decoupled-vs-coupled MISMATCH does NOT exist — both filter and test use the full coupled SE(3) tangent; see the doc-drift MINOR for the prose-vs-code wording gap, which the test resolves in favor of the code).
46× pessimism diagnosis — VERIFIED REAL, not a NEES bug. Independent arithmetic: init `P = I₁₂` (estimator.cpp:910, CONFIRMED `eskf.init(SE3{}, Mat12::Identity())`) ⇒ pose σ² ≈ 1 m²/rad²; steady-state `|e| ≈ 0.09` ⇒ `eᵀe ≈ 0.008`; with `P_pp ≈ I₆` the per-record NEES ≈ 0.008, and the distance-inflated `P_tt` (right-error Ad growth, eskf.cpp:64-67) pushes the translation contribution further down, landing the 6-DOF mean at ~0.13 — order-of-magnitude consistent with the OBSERVED 0.128893. A scaling/block/units bug would not produce this clean small value that tracks the verified `P=I` init; it would more likely overshoot DOF or be wildly off. The 6/0.129 ≈ 47× under-confidence ratio matches the reported ~46×. Verdict: covariance is genuinely over-conservative (NEES ≪ DOF), NOT a NEES scaling artifact.
Chi-square bound math — VERIFIED. Wilson-Hilferty `chi²_inv(p,k) ≈ k(1 − 2/9k + z√(2/9k))³` with k = N·6 = 145080, z = 2.5758 (test_validation.cpp:352-363) reproduces the live interval [5.94277, 6.05754] on the mean by hand to 5 digits; the interval is tight and correctly centered on DOF=6. The `N*nees ~ chi²(N*6)` aggregation is the right model under independence (see the autocorrelation MINOR for the one caveat — it widens, never narrows, the band, and cannot bridge 46×). `REQUIRE(N > 1000)` (:349; live N=24180) guarantees the band is non-degenerate.
Achievable band non-vacuous — VERIFIED. `[0.04, 0.40]` (test_validation.cpp:383-384) brackets the observed 0.129 with ~3× headroom each side; a 3× drift in either P or the tracking error breaks it (a real regression guard), yet it absorbs the M=30 seed-base variation. It is explicitly NOT the consistency interval and is documented as pinning the pessimistic value the filter emits today. `CHECK_FALSE(truly_consistent)` (:376) honestly pins the inconsistency so a silent flip-to-consistent (e.g. a future init-P fix) re-trips this test.
P_pp invertibility — SAFE. `Ppp.ldlt().solve(e)` (test_validation.cpp:130): P starts at `I₁₂` (PD) and predict-only `F P Fᵀ + Q` with PSD Q only grows it, so `P_pp` stays symmetric PD throughout — `ldlt` is stable, no conditioning risk; the pessimism (large P) makes it BETTER conditioned, not worse.
Golden regression genuineness — VERIFIED. PART 1 (test_validation.cpp:477-527) pins byte-identical replay of `frontier.pose` + full `frontier.cov` AND the per-source calib snapshot (extrinsic/scale/offset/3 confidences/3 commit flags) across two in-test runs WITH noise — extends test_sim's pose+cov-only determinism to calibration output; replay is genuinely deterministic because `mix_seed(seed,t0,t1)` is a pure function of the window (synthetic_source.cpp:127), not the call sequence. PART 2 (:529-595) pins committed numeric values (poses at fixed indices 10/40/300, max tracking error, source-1 scale 1.08281 + confidences) at tight tol (1e-5 pose, 1e-4 calib). The deliberate-perturbation claim is credible: 1e-5 absorbs ~1e-14 round-off + 6-digit literal rounding but a 6.4e-4 behavioral shift exceeds it by ~60×.
Noise-free portability — VERIFIED. The committed-value scenario passes `with_noise=false` (test_validation.cpp:535), and synthetic_source.cpp:116-120 gates the entire `std::normal_distribution` block behind a `noisy` flag, so the noise-free path is pure SE(3) arithmetic with NO `normal_distribution` — correctly avoiding the HANDOFF §6 cross-stdlib transform caveat. Live: `max_te,max_re < 1e-9` confirms machine-precision GT tracking, so the committed constants are exact arithmetic.
NIS deferral — HONEST and CORRECT. test_validation.cpp:54-62 documents NIS as uncomputable without measurement updates (no `ICorrection` until Slice 11, D22); the core IS predict-only (eskf.cpp:7, no correction step). Not faked, no placeholder NIS assertion. Matches DESIGN §10 / D24's NEES/NIS pairing being partially deferred.
Strict core / scope — VERIFIED. Diff touches ONLY `tests/test_validation.cpp` (NEW) and `tests/CMakeLists.txt` (one-line add at :23) — NO core or sim source modified (`git show 3822def --stat` confirms 2 files). Test is relaxed-edge (std::vector, unique_ptr, heap fine). The Q knobs used are TEST `q_scale`/`q_floor` (nees_config :137-159, golden_config), never core defaults.
Assertion-count jump — INTENTIONAL, acceptable (with the NIT above). The +11998 (5317→17315) is the determinism replay loop over all records×fields; it is correct (all byte-equality on deterministic data) and the suite stays fast (3.32s). Flagged as a NIT for proportionality, not a correctness defect.
Test quality / no vacuous assertions — VERIFIED. M=30 ensemble and run length (~24180 steady-state samples) bounded; suite 3.32s. The convention case probes the MAX-error record (live |e|=1.56) so the left-vs-right / raw-vs-SE(3)log distinctions are numerically meaningful (:228-235); `REQUIRE(e.norm() > 1e-6)` (:244) guards against a degenerate zero-error probe. The committed golden's idx-300 pin is a full 6-DOF pose, not a trivially-zero one. Determinism preserved (seeds fixed per run; pure-function noise).

TOTAL: 6 findings (0 CRITICAL, 0 MAJOR, 4 MINOR, 2 NIT) + 11 clean categories.
