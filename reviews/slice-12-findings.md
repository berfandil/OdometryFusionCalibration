# Slice 12 review — warm-restart persistence @ 799bb20

Scope: read the full diff (`git show 799bb20`) — `persistence.hpp` (NEW: explicit-LE/padding-free Writer/Reader + FNV-1a-64/32), `status.hpp` (+CorruptData/VersionMismatch), `estimator.cpp` (`config_hash()`/`serialize()`/`deserialize()` + the re-anchor restore), `test_persistence.cpp` (NEW, 6 cases), `tests/CMakeLists.txt` — plus the context they lean on: `estimator.hpp` (signatures), the Impl persisted state + `apply_calib_feedback`/`commit_gate_reanchor`/`update_commit_state`/`effective_offset` (estimator.cpp), `calibration.hpp` (set_basepoint/reset_so3/reset_scale/reset_roll/reset_lever/set_yaw_pitch contracts), `timesync.hpp` (`commit_gate`), `config.hpp`+`SensorConfig`, `types.hpp` (SE3). Against DESIGN §7/§9, DECISIONS D23/D6/D8, ISSUES Slice-12 done-criteria, HANDOFF §5/§6, reviews/slice-11/10-findings.md (assertion-proportionality precedent). Built + ran full gate GREEN: **193 cases / 8241 assertions, 0 failed** (ctest 1/1, 6.7 s; cosmetic `vswhere.exe`/`LF→CRLF` lines ignored); persistence subset = 6 cases / 157 assertions; near-NOMINAL live numbers **warm=0.00104 m vs cold=0.256 m** (247× gap, warm is absolutely ~1 mm = genuinely near-NOMINAL, not merely relatively better). VERDICTS: (a) FORMAT/PARSE SAFETY — SOUND, NO OOB-before-checksum: deserialize validates `len == 20 + payload_len + 4` EXACTLY (line 1407) BEFORE computing the checksum over `covered = 20 + payload_len` (1409-1410), and the trailing word is read from `buf+covered` (in-bounds since `covered < len`); magic→version→length→checksum→config-hash order is correct; doubles round-trip bit-exact via IEEE-754 bit pattern; LE explicit on both write and read (host-endian-independent). (b) CONFIG-HASH COMPLETENESS — ADEQUATE, no MAJOR silent-stale-restore omission: every field the restore actually injects (per-sensor priors/flags/scale_calib, the 5 histograms, commit/gate thresholds, rig shape) is covered; the excluded knobs (tick_rate_hz, calib_lag_s, fusion_delay, reliability clamps, fusion-math knobs) are either re-clamped/re-derived each step or shape only future convergence, so none enables an incompatible stale restore — the documented exclusion is defensible (one consistency NIT below). (c) RE-ANCHOR-AND-REFILL RESTORE — genuinely resumes near-NOMINAL with NO revert window for the ext/roll/lever/scale DOFs (priors are set directly in deserialize and `apply_calib_feedback` never reverts a prior to the config value; `commit_gate_reanchor(prev=true)` holds the restored flags through the empty histogram), BUT the OFFSET DOF transiently re-opens (`update_commit_state` uses plain `commit_gate`, not the re-fill variant) — the offset VALUE survives (folded into prior_time_offset) so fusion is unaffected, but `CalibSnapshot::committed` reads false until the time-sync histogram refills (a consumer-visible readiness/distinction loss, the offset analog of the histogram-bins deferral). HISTOGRAM-BINS DEFERRAL JUDGEMENT: ACCEPTABLE documented deviation from DESIGN §7's literal "histogram bins" wording — it restores the committed VALUES + holds them via hysteresis and loses only the in-progress vote distribution + the confidence reading (readiness reads low until refill despite correct values); it is a §7 doc-vs-impl reconciliation for the orchestrator, not a correctness gap. Gate result: SUCCESS.

src/core/estimator.cpp:862,1484 (+1476-1479): MINOR - OFFSET DOF re-opens for the first refill window after restore. deserialize restores `offset_committed[slot]=true`, but the NEXT step()'s `update_commit_state()` (line 862) recomputes it via the PLAIN `commit_gate(prev=true, timesync.confidence≈0 over the empty post-restore histogram, ...)` which returns `confidence >= commit_drop` → FALSE, flipping the flag off until votes re-accumulate. The offset VALUE is safe (it was folded into `prior_time_offset` and `effective_offset()` returns it when uncommitted), so fusion stays near-NOMINAL — but `CalibSnapshot::committed` (estimator.cpp:1200) reads FALSE for the restored offset until refill, unlike the ext/roll/lever/scale flags which are held committed by `commit_gate_reanchor`'s vote-mass re-fill guard. A consumer keying readiness off `committed` sees the offset regress to "uncommitted" despite the correct value. Documented at 1476-1479, so a known limitation, not a hidden bug. Fix: either route the offset commit through a `commit_gate_reanchor`-style re-fill guard (hold `prev=true` through votes<N_min) on restore, or persist+restore the offset histogram, so `committed` reflects the restored state immediately.

src/core/estimator.cpp:140,153-218: MINOR - config_hash OMITS `calib_lag_s` + `tick_rate_hz` while the header comment (138-140) claims it covers "the commit/gate thresholds that shape how calibration converges" and excludes only knobs that "don't change what a persisted calibration MEANS (tick rate, fusion delay...)". But `calib_lag_s`·`tick_rate_hz` = the smoother lag L (estimator.cpp:741) — a CALIBRATION-shaping knob, and `phase2_strategy` (a sibling calibration knob) IS hashed, so the inclusion line is inconsistent. Not a stale-restore corruption (restore injects no smoother/ring state and L only affects the future deeper frontier), hence MINOR not MAJOR. Fix: either hash `calib_lag_s` (and accept `tick_rate_hz` as the documented runtime exclusion) for consistency with `phase2_strategy`, or tighten the comment to state that the deeper-frontier lag is deliberately excluded because no lag-dependent state is persisted.

include/ofc/core/persistence.hpp:161-167: NIT - `get_se3()` reconstructs R from the 9 stored doubles with NO re-orthonormalization. For a checksum-valid blob this is exactly correct (bit-identical to the orthonormal matrix that was written, so it IS orthonormal). But a corrupt blob whose flipped bytes happen to keep the FNV-1a-32 checksum valid (a ~1-in-4e9 non-crypto collision) would restore a non-orthonormal R that then propagates into `X∘B∘X⁻¹`. Cosmetic given the checksum + config-hash double guard; note only. Fix (optional): a cheap `R = nearest-orthonormal(R)` (or a `det≈1 ∧ RᵀR≈I` assert returning CorruptData) on the restored extrinsic would close the residual-collision path.

src/core/estimator.cpp:1404: NIT - the `payload_len > 0x40000000` sanity cap is a magic constant with an inline `// sanity` comment but no named rationale tying it to the caller buffer bound. It is harmless (the subsequent exact `len == need` check is the real guard, and 0x40000000 only short-circuits an absurd length before the `need` arithmetic), but a named constant (e.g. `kMaxPayloadLen`) documenting "well above the 32-source ≈ 4 KiB max, below INT overflow in `need`" would make the intent self-evident. Note only.

tests/test_persistence.cpp:283: NIT - the near-NOMINAL assertion `warm < cold * 0.6` is far looser than the observed 247× margin (warm 0.00104 m vs cold 0.256 m). The case is NOT rigged — the short-window isolation is sound (cold-start cannot clear commit_min_votes=60 in ~3 s of a 1/3-straight trajectory, verified by the separate `scale_committed`-only-via-restore assertion at :294-295) and the absolute warm error ~1 mm independently proves near-NOMINAL — but a tighter bound (e.g. `warm < 0.01` AND `cold > 0.1`) would assert the near-NOMINAL PROPERTY directly rather than only a relative gap that 0.6 under-claims. Count/clarity hygiene only; all pass.

DESIGN.md:§7 / DECISIONS.md:D23 / HANDOFF.md:62: MINOR - DOC DRIFT (note, do not fix). DESIGN §7 and D23 both list "**histogram bins**" explicitly among the persisted calibration state ("persist calibration state (committed extrinsics/scale/offsets, histogram bins, reliability weights, lifecycle confidence)"). The impl deliberately does NOT persist bins (re-anchor-and-refill — documented at estimator.cpp:1302-1314). This is an approved, sound deviation (see Scope verdict (c)), but §7/D23 should be reconciled: either reword "histogram bins" to "committed calibration VALUES + flags (bins re-fill via re-anchor)" or add a "Slice-12 impl: bins NOT persisted; re-anchor-and-refill" note, so the spec matches what shipped. Also note the two new Status codes (`CorruptData`/`VersionMismatch`, status.hpp:20-21) are not yet reflected in any status-enum doc.

CLEAN categories (verified, no finding):
SERIALIZATION FORMAT CORRECTNESS — VERIFIED. Explicit little-endian byte-by-byte for u32/u64/i32 (persistence.hpp:91-101), bytewise on BOTH write (shift-out) and read (shift-in 138-150) so it is host-endian-INDEPENDENT. Doubles via the IEEE-754 bit pattern through `std::memcpy` to/from `uint64` (106-110, 155-160) — round-trips BIT-EXACT (double everywhere, no fast-math), confirmed by the round-trip + determinism tests' byte-`memcmp`. SE3 written row-major 9 R + 3 t and read back in the identical (r,c) order (112-116, 161-167) — symmetric, bit-exact, so the reconstructed R is exactly the orthonormal matrix written (the corrupt-collision caveat is the NIT above). Writer/Reader guards: `put_*`/`get_*` each check `fits`/`has` and trip `overflow`/`underflow` then no-op / zero-fill (86-90, 133-137) — no OOB write on an oversized record, no OOB read on a truncated buffer; caller checks the flag once (serialize 1363/1373/1378, deserialize 1393/1401/1453).
CHECKSUM + VERSION + MAGIC ORDER — VERIFIED, NO OOB-BEFORE-CHECKSUM. Order is magic (1394-1396, rejects before any further parse) → version (1397-1398) → header underflow (1401) → payload_len sanity + EXACT `len == 20+payload_len+4` (1404-1407) → checksum over `covered=20+payload_len` (1409-1413) → config-hash (1416). The length is validated against the actual `len` BEFORE the checksum loop, so `fnv1a32(buf, covered)` and the `buf+covered` trailing read are both in-bounds for any input; a corrupt/oversized payload_len cannot drive an OOB read ahead of the checksum. Checksum is computed over header+payload EXCLUDING the trailing 4-byte word (serialize 1376 over `w.pos` before writing the checksum; deserialize 1409-1410 over `covered`) — scoped correctly. FNV-1a-32 is a non-crypto checksum (corruption detection only) — appropriate for the stated crash/flip-detection role, not security; correctly scoped.
CONFIG-HASH GUARD — VERIFIED adequate. Covers rig shape (max_sources/reference_sensor_id/buffer_capacity/min_sources_warn/cold_start), commit+gate thresholds, regime gates, phase2_strategy, timesync_enabled/match_metric/max_lag_s, all 5 histogram configs, and the full per-sensor record (id/prior_extrinsic/prior_scale/prior_time_offset_s/weight_prior/noise models/kf_process_noise/native_confidence/per_sensor_kf/scale_calib/bias_states/is_reference). Deterministic + order-stable: sensors iterated 0..nsens in array order (194), same explicit-byte (Writer) encoding as the payload so padding-free + endian-explicit. Sensors-beyond-32 cannot be silently dropped (validate enforces sensor_count ≤ max_sources ≤ 32; the nsens clamp + the overflow-fold at 211-217 are belt-and-suspenders). +0/-0/NaN hash by raw bits → two numerically-equal-but-bit-different configs hash differently = CONSERVATIVE (never a false-accept). The three rejection cases (moved prior, changed reference, changed bins) all reject InvalidConfig and the same-config case accepts (test :302-344). The omission NIT/MINOR is the calib_lag_s line above; no load-bearing field whose change would let stale state restore into an incompatible rig is missing.
RESTORE CORRECTNESS (re-anchor-and-refill) — VERIFIED, no revert window for ext/roll/lever/scale. deserialize restores prior_extrinsic/prior_scale/prior_time_offset + the 5 commit flags + resid_mean/var/reliability/resid_n + lifecycle (1470-1490, 1515-1524), then re-anchors calib1 (set_basepoint+reset_so3+reset_scale) and calib2 (set_basepoint+reset_roll+reset_lever+set_yaw_pitch) to the restored extrinsic (1498-1508) — exactly the Slice-8 commit re-anchor. The fusion prior is written DIRECTLY in deserialize and `apply_calib_feedback` only ever WRITES a prior when committed (never reverts to the config value), and `commit_gate_reanchor(prev=true)` returns true through votes<N_min, so the restored ext/roll/lever/scale DOFs hold committed + fusion uses the restored priors from step 1 (round-trip test :229-233 + near-NOMINAL :291-295 confirm `scale_committed` + scale≈1.20 after ONE step). Atomic restore: payload parsed into LOCAL `Rec[]`, committed to Impl only after `pr.underflow` is clear (1418-1453). Unregistered persisted ids skipped, unpersisted registered sources keep their cold config prior (1457-1466) — correct. prior_scale/resid_var/resid_n re-sanitized on restore (1471/1488/1490). The offset-distinction loss is the MINOR above.
STRICT CORE — VERIFIED. serialize/deserialize touch ONLY the caller buffer + a fixed STACK scratch (`payload[4096]` serialize 1337, `Rec recs[32]` deserialize 1436, `scratch[8192]` config_hash 157) — no heap, no exceptions, no file IO in core. Loops bounded by source_count ≤ kMaxSourcesCap=32. Status/Expected returns on every path (NotInitialized/CapacityExceeded/CorruptData/VersionMismatch/InvalidConfig/Ok). persistence.hpp is header-only, fixed-size structs, zero allocation. The double-buffer ping-pong + fstream IO lives entirely in the TEST (test_persistence.cpp:431-500, relaxed edge); grep-confirmed no `<fstream>`/file IO leaked into src/core. Production file adapter correctly deferred to Slice 13.
DETERMINISM + ASSERTION PROPORTIONALITY — VERIFIED. Two identical converged runs serialize byte-identical (`memcmp` :564-570) and a re-serialize of a restored estimator equals the original blob (:241-246) — config_hash + payload are pure functions of state. Assertion budget proportionate: 157 assertions across 6 cases, no per-record spam (the byte-equality checks are single folded `memcmp`s, matching the Slice-14/11 precedent); framing rejections are one CHECK per case. The double-buffer test cleans up its temp files (:557-558) and uses a tagged temp path (:494-499).
VALIDATE / STATUS — VERIFIED. New `CorruptData`/`VersionMismatch` (status.hpp:20-21) are well-scoped (bad magic/checksum/truncation vs format-version skew) and used consistently. serialize null/`cap<0`→CapacityExceeded (1331), deserialize null/`len<0`→CorruptData (1385), both NotInitialized pre-init (1330/1384) — all exercised by test (g) :410-418.

TOTAL: 7 findings (0 CRITICAL, 0 MAJOR, 3 MINOR, 4 NIT) + 7 clean categories.
