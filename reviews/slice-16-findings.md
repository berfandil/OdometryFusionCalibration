# Slice 16 review — centroid sub-bin readout @ c237bfb

VERDICT: APPROVE — implementation matches the spec (SLICE16_CENTROID_SUBBIN_READOUT.md §2/§3) on every review axis; no blockers, no majors. Counts: 0 BLOCKER, 0 MAJOR, 1 MINOR, 3 NIT.
Verified independently: the simplified formula `c + (hp−hm)/Σm·w` IS the exact 3-bin mass-weighted centroid with unwrapped centers (Σmᵢcᵢ/Σmᵢ with c±w substituted, c·Σm factored out), including the non-circular edge — `mass_at` returns 0 for the missing neighbor, which removes it from BOTH numerator and denominator, so the formula equals the one-sided 2-bin centroid (hand-checked: vote 0.7 → 0.5+0.2/1.0=0.7; vote 9.3 → 9.5−0.2=9.3; seam votes 7.95/8.05/0.0 → exact, peak-at-bin-0 circular case included via 8.05/0.1). Offset is inherently bounded ±0.5w (h0 is the argmax ⇒ |hp−hm| ≤ max(hp,hm) ≤ h0 ⇒ |off| ≤ 0.5), so the missing clamp is safe; circular fold handles the only out-of-range case (m=range_max → range_min, circularly correct). Empty path (midpoint) and subbin=false path untouched and re-pinned; the parabolic branch is unrestructured (block inserted before it, its own hm/h0/hp scoped inside the new if — default-off bit-identical, pinned with EXACT == on the planted (2,8,6) triple). Strict core holds (stack-only, no exceptions, O(nbins) bounded, Scalar=double). Config-hash addition sits inside `hash_histogram` so all five histograms are covered by the existing five call sites; put_bool→put_u8(1/0) is sane. Loader key is in the [global] chain, fans out to all five hists, rejects malformed bool, knob_doc + header key-list updated. confidence()/commit gates/median/ESKF untouched (estimator.cpp diff is hash-only). Gate re-run by reviewer at c237bfb: ctest 2/2 passed (adapters 0.52s, unit 15.13s).

tests/test_persistence.cpp:439: MINOR: the new config-hash guard test pins only the REJECTION direction (blob written with subbin_centroid=false restored into a true-flag rig, cases a+b); there is no positive control that a blob written WITH subbin_centroid=true restores into an identical true-flag rig (Status::Ok) — same-flag hash EQUALITY on the new field is unpinned, so a hash that rejected everything (or encoded the flag unstably) would still pass this test (the pre-existing same-config restore tests run flag=false on both sides only). fix: add case (c): set scale_hist.subbin_centroid=true before converge+serialize AND on the restoring rig, expect deserialize == Status::Ok.

include/ofc/core/histogram.hpp:4: NIT: the class-level doc still says the primitive "extracts a sub-bin peak (parabolic interpolation)" with no mention of the opt-in centroid readout — the .cpp layout comment and config.hpp were updated but the header API doc was not (same staleness at the histogram.hpp:108 `mass_at` comment "Bin mass for the parabola / confidence", which now also serves the centroid). fix: add one clause "(parabolic interpolation, or the opt-in mass-weighted centroid over peak±1 — subbin_centroid)" and widen the mass_at comment.

tests/test_histogram.cpp:562: NIT: the acceptance-1 exactness loop only sweeps NON-NEGATIVE fractions {0,.1,.25,.4,.5} — every interior linear-case vote puts the split mass in the RIGHT neighbor (hp-path); the hm-path (vote BELOW the peak-bin center) is exercised only indirectly via the seam (0.4, 8.05) and upper-boundary (9.3) cases, not in the plain interior loop the acceptance item describes. fix: extend the loop with negative fractions (e.g. −0.1, −0.25, −0.4) so a sign error in `(hp − hm)` is caught by the headline acceptance case itself.

tests/test_histogram.cpp:700: NIT: two config-space interactions of the new flag are unpinned: (a) vote_split=false + subbin_centroid=true (a single unsplit vote should read the bin center — masses carry no sub-bin encoding; sensible by construction but untested), and (b) centroid under Decay aging with a multi-vote history (uniform γ-scaling cancels in the mass ratio, so the centroid is decay-invariant — low risk, but the invariance is exactly the property worth one cheap pin since all five production hists may run Decay). fix: add one small case each (split=false single vote → Approx(bin center); two decayed votes → centroid still the analytic mass-weighted mean).
