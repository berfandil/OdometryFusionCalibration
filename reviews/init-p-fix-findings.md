# init-P covariance fix review — seed P from first window @ e13b1fc

Scope: read the full diff (`git show e13b1fc` / `--stat`: 2 files, src/core/estimator.cpp first-fuse seed + tests/test_validation.cpp NEES band/comments) in context against src/core/eskf.cpp (`init`/`predict` Qmap/`adaptive_q`), include/ofc/core/eskf.hpp, src/core/estimator.cpp (the dt + q_pose derivation, the first-fuse path), include/ofc/core/config.hpp (q_floor/q_scale), DESIGN §5/§7/§10, DECISIONS D21/D22/D24, reviews/slice-14-findings.md (original NEES math + pessimism diagnosis); independently grepped every `.cov`/NIS/gate/golden/`eskf.init` assertion across tests/ + adapters/tests/. Built + ran the full gate GREEN: ctest 2/2 (adapters 0.31s, unit 7.63s); unit 195 cases / 8274 assertions 0 failed; adapters 20 cases / 319 assertions 0 failed. Observed LIVE: ensemble-mean pose NEES = 0.347718 (N=24180, chi2 99% interval [5.94277, 6.05754], truly_consistent=NO); NIS = 2.39085 (N=4860, DOF 3); Mahalanobis gate test: inliers applied=78, outliers rejected=5, max NIS rejected=307052, in-window err 0.0285m. VERDICTS: (a) seed correct — symmetric PSD, EXACTLY matches predict()'s Qmap twist mapping, applied first-fuse-only, dt + q_pose valid/in-scope; (b) NEES improvement REAL (0.348 reproduced live, not a test artifact), test update HONEST + non-vacuous (lower bound 0.22 re-trips on a revert to I_12 which collapses to ~0.13); (c) residual-pessimism diagnosis CORRECT (translation block accumulates Ad-transport + Q while rotation stays tight; seed cannot touch it). One NIT below; no CRITICAL/MAJOR/MINOR.

src/core/estimator.cpp:1280-1285: NIT - the seed `P_init` block(0,0)=q_pose is applied by `init()` AND then `predict()` (line 1285) immediately adds another `Q=blkdiag(q_pose, q_pose/dt^2)` for the SAME first window, so after the first fuse the pose block is ~Ad*q_pose*Ad^T + q_pose ~= 2*q_pose — the first window's process noise is effectively counted twice (seed says "the filter starts as if it integrated its first window", then it integrates that window again via predict). Magnitude is negligible here (q_pose ~ q_floor ~ 1e-6 dominated by the distance-driven Ad-inflation, so the live NEES 0.348 is unaffected) and the seed is physically defensible either way (gauge-anchored pose ~ near-certain), so this is cosmetic. If a tighter early-time P is ever wanted, either seed P=0 and rely on the first predict to add the one window, or skip the first predict's Q on the seeded step; not worth changing now — note only.

CLEAN categories (verified, no finding):
1. SEED CORRECTNESS (PSD + matches predict mapping + first-fuse-only) — VERIFIED. `P_init=blkdiag(q_pose, q_pose/dt_eff^2)` (estimator.cpp:1279-1281) is byte-for-byte the same Qmap predict() builds (eskf.cpp:79-81: pose=q_pose, twist=q_pose/dt_eff^2). Symmetric PSD: q_pose is PSD (adaptive_q = q_scale*spread^2*I + diag(q_floor>0), eskf.cpp:171-176); /dt^2 (dt>0) preserves PSD; eskf::init symmetrizes (eskf.cpp:48). Twist block uses the identical 1/dt^2 delta->velocity mapping. q_pose is the adaptive Q for THIS fuse (estimator.cpp:1248-1253, computed once) and dt is the first window's elapsed (estimator.cpp:863), both in scope at the seed. Applied ONLY at the first fuse: gated by `if(!s.eskf_started)` then latched `s.eskf_started=true` (estimator.cpp:1277,1283). Sole production call site of eskf.init that changed (the only other init() callers are eskf unit tests in test_eskf.cpp, which exercise the Eskf class directly and are unaffected by the estimator change).
2. dt VALIDITY at the seed — VERIFIED. dt is computed at estimator.cpp:863 AFTER the `if (t1 <= q0)` degenerate-interval guard returns NotReady (estimator.cpp:858-862), so dt > 0 is guaranteed by the time line 1278 runs. The `dt_eff = (dt>0)? dt : 1e-9` fallback is therefore dead-but-harmless belt-and-suspenders (mirrors eskf.cpp:54's own guard). No stale/zero dt reaches the seed.
3. NEES IMPROVEMENT REAL + TEST HONEST — VERIFIED. Live ensemble-mean = 0.347718 (matches the reported 0.348), up from the slice-14-verified 0.128893 under I_12 — a genuine ~2.7x improvement attributable to the seed (removes the I_12 pose-block over-sizing: ~1 rad^2/m^2 vs true rot ~0.14 / early trans ~0.008). Not a test artifact: the mean-propagation (and thus tracking error) is independent of P on the predict-only path, only the published Sigma changed. `CHECK_FALSE(truly_consistent)` is honest (0.348 << 6; live verdict NO).
4. NEES BAND NON-VACUOUS + REGRESSION GUARD — VERIFIED. [0.22, 0.50] brackets 0.348 with ~1.5x headroom; the lower bound 0.22 genuinely re-trips on a revert to the I_12 seed (which the slice-14 review measured at 0.129 < 0.22) — a real anti-regression. Loose enough to absorb the M=30 seed-base variance (0.348 sits mid-band), tight enough that a ~1.5x drift in either P or tracking error breaks it. Not the consistency interval; documented as pinning the emitted pessimistic value.
5. RESIDUAL DIAGNOSIS (predict-only translation Ad-inflation) — VERIFIED CORRECT and ~0.348 is the EXPECTED post-seed value, not a further bug. The seed only fixes the t=0 magnitude (negligible q_pose); the dominant pessimism is the cumulative translation-block growth (each predict transports P through Ad(delta^-1) — whose [t]xR off-diagonal injects rotation uncertainty into translation — and adds Q, with no correction to shrink it on the no-ref path). The rotation-rotation block of Ad is the isometry R^-1 (no inflation) so rotation stays ~0.14 rad^2 (matches the test comment) while P_tt climbs to O(few) m^2 (slice-14 review independently saw O(3..20) m^2). The "over distance" wording is a slight simplification (it is cumulative-step accumulation, not a single-step |t| term — the Ad uses the per-WINDOW delta, whose |t| is small) but the conclusion — translation block dominates the residual gap, rotation is well-calibrated, seed cannot touch it — is sound and matches the live evidence.
6. NON-REGRESSION across cov-dependent tests — VERIFIED, nothing else needed changing. (a) test_sim.cpp:631-642 + test_validation.cpp:693-695 determinism: compare cov between two SAME-config runs (both use the new seed) -> still byte-equal. (b) test_validation.cpp golden PART 2 (:753-786): pins only fused POSE + calib snapshot, NO cov magnitude; pose mean is P-independent on the predict-only path -> golden constants unaffected (live max_te,max_re < 1e-9 still). (c) NIS Monte-Carlo (:489) sets mahalanobis_chi2=1e9 (gate disabled) so a smaller P cannot change acceptance; live NIS 2.391 unchanged because the correction sets P from the measurement noise within a few fixes (seed-insensitive — verified the reasoning). (d) Mahalanobis gate test_correction.cpp:190-244 (chi2=9): live inliers=78 accepted, 5 outliers rejected at NIS 307052, in-window err 0.0285m — smaller P does NOT reject inliers and the gross 30m outlier still trips (the implementer's reported numbers reproduce exactly). No silent wrong-expected: the cov-dependent assertions are all robust bands or two-run equalities, none a single-run cov magnitude pin.
7. test_eskf.cpp init(Mat12::Identity()) calls — VERIFIED unaffected. The 13 `f.init(SE3{}, Mat12::Identity()/...)` calls in test_eskf.cpp drive the Eskf CLASS directly (unit tests of init/predict/update math), which the diff did not touch; the seed change is purely in estimator.cpp's first-fuse path. The "one predict pins covariance to F P0 F^T + Qmap" analytic case (test_eskf.cpp:91-141) still passes (its P0 is the test's own Identity, not the estimator seed).
8. STRICT CORE / SCOPE — VERIFIED. Seed is heap-free (fixed Mat12, Mat6 blocks), bounded, double, symmetric PSD; localized to the first-fuse init only; no new config knob (derived from the existing q_floor/q_scale via q_pose + the first-window dt — defensible: the seed IS the one-window process noise the same knobs already govern, so exposing a separate init-P knob would duplicate them). Core math (median/Weiszfeld, calibration, smoother) untouched (diff stat: estimator.cpp + test only).
9. DETERMINISM — VERIFIED. The seed is a pure deterministic function of the first window (q_pose from spread + q_floor/q_scale; dt from t1-q0), so replay is byte-stable; the determinism cases (test_sim + golden PART 1) pass for the right reason (same-config replay equality, both runs identically seeded P).
10. DOC DRIFT (note, do not fix): DESIGN §10 (line 142) still states "ensemble-mean NEES ~= 0.13 vs DOF 6 (~46x)" + "Fixing it needs a smaller init-P seeded from the first-window" as an OPEN item — now resolved (NEES 0.348, ~17x, residual = predict-only translation Ad-inflation). DECISIONS D21 impl-note (line 109) likewise still says "mis-calibrated (pessimistic ~46x)". HANDOFF init-P item (per the brief, §6/§7) should be marked done. Not fixed (review-only).

TOTAL: 1 finding (0 CRITICAL, 0 MAJOR, 0 MINOR, 1 NIT) + 10 clean categories.
