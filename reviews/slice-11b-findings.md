# Slice 11b review — per-source bias states (augmented ESKF, Option A) @ 48ece29

Scope: read the full diff (`git show 48ece29`) — `types.hpp` (Vec18/Mat18), `config.hpp` (`SensorConfig::bias_process_noise`), `eskf.hpp` + `eskf.cpp` (`enable_bias`/`bias`/`bias_pose_coupling`/`bias_confidence`/`predict_aug`/`update_aug`), `estimator.cpp` (validate guard + `config_hash` + `add_source` detect + the step() routing/snapshot), `result.hpp` (`CalibSnapshot::bias`/`bias_observable`), `synthetic_source.hpp`/`.cpp` (`body_twist_bias` plant), `test_bias.cpp`, `tests/CMakeLists.txt` — plus the math it relies on in context: the 12-DOF `predict()`/`update()` baseline (eskf.cpp), `lie.cpp` (`se3::exp`/`log`/`adjoint`, `so3::left_jacobian`), `absolute_ref.hpp` (`Measurement` 6x12 H). Against DECISIONS D22/D21/D4/D8, DESIGN §5/§8, ISSUES Slice 11b DESIGN NOTE (Option A spec), HANDOFF, reviews/slice-11/14/10-findings.md (assertion-proportionality precedent). Built + ran the FULL gate GREEN: ctest **2/2**, unit **199 cases / 10519 assertions, 0 failed** (+4 bias cases), adapters **20 / 319, 0 failed** (cosmetic `vswhere.exe`/`LF→CRLF` lines ignored). Live numbers reproduced: bias OFF tail err **16.08 m** -> ON **0.056 m** (~286x), recovered **[0.1431, -0.042, -0.085, -0.027, -0.005, 0.0482]** vs planted **[0.15, 0,0,0,0, 0.05]** (observable DOF 0/5 ≈ 5%/4% off), max `bias_observable` **0.996**, fixes applied **111**; no-ref self-test: bias norm **0**, `bias_observable` **0**. VERDICTS: (a) **`J_pb = -dt·I₆` is CORRECT** — independently re-derived the post-update right-tangent Jacobian numerically: `J = -dt·Ad(exp(b·dt))`, of which `-dt·I` is the exact leading term and the omitted correction is **O(dt²·‖b‖)** (residual ratio/dt² constant at 0.114; with dt≈0.05, b≈0.15 the error is ~4e-5 — negligible). The `Ad(Δ_db⁻¹)` factor floated in the brief is WRONG (off by 7.8e-2; it would inject a large O(‖logΔ‖) error since Δ is not small). The ~5% recovery gap is **observability/GPS-noise/finite-run, NOT a Jacobian error**. (b) **Default-OFF IS byte-identical** — `bias_slot=-1` ⇒ `bias_drives_alone` always false ⇒ `predict_aug`/`update_aug`/`enable_bias` never entered, 12-DOF path verbatim, sim plant zero-guarded; the 195 pre-existing cases are untouched (195→199 by ADDING 4). (c) **Augmented predict/update are PSD + sound** — block-diag PSD Q + Joseph form + symmetrize18 on both paths; seeding sane.

src/core/estimator.cpp:1340-1347: MINOR - OUT-OF-REGIME FALLBACK leaves `cov18_` STALE and DROPS the de-bias. When the augmented mode is already enabled (`bias_active_`) but the bias source stops driving alone (n>1), step() calls the 12-DOF `s.eskf.predict(med.value, ...)`, which advances `state_.cov` (12x12) but NOT `cov18_`, AND integrates the RAW `med.value` (the learned bias is NOT applied to the de-bias). Two consequences: (1) the learned bias stops helping the trajectory the instant another source joins; (2) if the bias source later drives alone again, `predict_aug` resumes from a `cov18_` whose top-left 12x12 and pose↔bias cross-cov are stale (un-propagated across the gap) — a covariance inconsistency (no dimension mismatch / crash, but a corrupted prior). Cannot occur in the two documented Option-A regimes a SINGLE-source rig keeps `bias_drives_alone` true every step (the tests), so this is dead-but-guarded TODAY but a latent bug for the 2-source `ReferenceOnly` rig the routing comment (estimator.cpp:1321-1322) explicitly admits as in-regime. Fix: either (a) when `bias_active_` and falling back, still apply the frozen bias to the de-bias and mirror the 12-DOF predict into `cov18_`'s top-left block (zeroing the now-meaningless cross-cov), or (b) tighten the doc to state the augmented mode is only supported for a strictly 1-source-median rig and reject/disable it otherwise.

tests/test_bias.cpp:193-194: NIT - the convergence tolerances `epsilon(0.5)` (v_x) and `epsilon(0.6)` (omega_z) are LOOSE: they admit a recovered value anywhere in [0.075, 0.225] / [0.02, 0.08], so a substantially mis-scaled `J_pb` would still pass this CHECK. The actual recovery is ~5%/4%, and the sign checks (:197-198) + the 286x drift-reduction (:201) provide additional pinning, but the comment at :191-192 claims the test proves "the J_pb Jacobian is correct" — the loose tolerance under-tests that claim. Tighten to e.g. `epsilon(0.15)` (the observed run has ample margin) so a constant scale/frame error in `J_pb` would actually fail, or soften the comment to "convergence to the right sign/order-of-magnitude" (which is what it tests).

tests/test_bias.cpp:269-285: NIT - assertion proportionality (the Slice-11/14/10 precedent, as flagged). The bias suite is 4 cases / **2245 assertions** (~561/case); **2221** of them are in case (c), the byte-identity test one `CHECK(equal)` per fused record (good — the 5 field comparisons are already folded into ONE boolean per record), but across ~1100 fused records x 2 runs. This re-asserts the same "byte-identical" property ~2200 times. The fold is the right shape; the remaining spam is the per-record loop itself. Recommend digesting the two runs into a per-run hash / a single accumulated mismatch counter checked once (`REQUIRE(mismatches == 0)`), keeping the assertion budget proportionate to the property. Not a correctness issue (all pass; suite stays fast at 7.2s).

src/core/eskf.cpp:77-81 / include/ofc/core/eskf.hpp:57-63: NIT - DEAD CODE. `bias_pose_coupling()` is declared + defined but has ZERO callers (grep-clean across src/, tests/, sim/) the snapshot uses `bias_confidence()` instead (the bounded "determined" signal), as the header docstring itself recommends. Either wire it into a diagnostic or drop it to keep the surface lean (it is documented as "the mechanism term", so a one-line note that it is exposed-for-future-diagnostics is acceptable if intentional).

include/ofc/core/result.hpp:66: NIT - DOC DRIFT (note, do not fix). The one-line field comment `Scalar bias_observable = 0.0;  // pose<->bias cross-cov magnitude (Slice 11b)` CONTRADICTS both the struct doc-block above it (:48-55, correctly "1 - P_bias/P_bias_prior, reduction of variance") AND the implementation (`bias_confidence()`, eskf.cpp:83-88). The same stale "cross-covariance magnitude" wording appears in the inline comment at estimator.cpp:1396-1399 (the code there calls `bias_confidence()`, not the cross-cov). Align both inline comments to the reduction-of-variance semantics.

DESIGN.md:89 / DECISIONS.md:114-115 / ISSUES.md:99: NIT - DOC DRIFT (note, do not fix). DESIGN §5:89 still reads "**DEFERRED to Slice 11b** ... `SensorConfig::bias_states` is currently a no-op (the core 12-DOF state is NOT augmented)"; DECISIONS D22 impl-note (:114) likewise says "Bias states + GPS adapter DEFERRED to Slice 11b — the core 12-DOF state is NOT augmented; `bias_states` is a no-op"; ISSUES Slice 11b box is `[ ]`. All three are now stale (11b Option A shipped: state IS augmented to 18-DOF when a bias source is active, `bias_states` drives the filter). The orchestrator should mark 11b `[~]` (Option A done, Option B + per-n χ² gate deferred) and update the D22/§5 impl-notes. Note: test_correction.cpp:14 comment ("Slice 11b — `bias_states` stays a no-op flag") is also now stale.

CLEAN categories (verified, no finding):
J_pb JACOBIAN (TOP PRIORITY) — VERIFIED CORRECT. Independently re-derived the post-update right-tangent Jacobian `∂δpose⁺/∂δb` numerically (central-difference, codebase exp/log/adjoint conventions, [trans;rot] order, T_true=T∘exp(η)): the exact value is `-dt·Ad(exp(b·dt))`; `J_pb = -dt·I₆` is its EXACT leading term, omitted correction `-dt²·ad(b)` is **O(dt²·‖b‖)** and vanishes as O(dt²) (verified: residual/dt² constant at 0.1135 across dt∈{0.2,0.1,0.05,0.025}). The pose↔bias term and the `Ad(Δ_db⁻¹)·δpose` transport are in the SAME post-update right tangent (no frame mixing — confirmed by deriving η via `log(pose⁺⁻¹ ∘ pose⁺_pert)`). The brief's two alternative hypotheses are both worse: `-dt·Ad(Δ_db⁻¹)` is off by 7.8e-2 (WRONG — Δ is large), and there is no left-Jacobian-of-exp(-b·dt) factor at leading order. So `J_pb` is a justified first-order approximation, not a sign/scale/frame error.
AUGMENTED PREDICT MATH — VERIFIED. Mean `pose ∘ (Δ∘exp(-b·dt))`, twist re-read `log(Δ_db)/dt`, bias mean held (eskf.cpp:201-207). `F`: pose row `[Ad(Δ_db⁻¹) | 0 | -dt·I]`, twist row 0, bias row `[0|0|I]` (:212-219) — matches the 12-DOF F plus the coupling + random-walk. `Q = blkdiag(q_pose, q_pose/dt², bpn·dt·I)` (:221-225) — same Qmap as predict() plus the bias RW; each block PSD, block-diagonal ⇒ Q PSD. `cov18_ = symmetrize18(F P Fᵀ + Q)` (:227) PSD by construction. De-bias linearization point is consistent between mean (Δ_db) and Jacobian (Ad(Δ_db⁻¹) + leading -dt·I about current b). `bpn` guarded `>=0`, dt<=0 guarded as in predict().
AUGMENTED UPDATE MATH — VERIFIED. `H` 6x18 = `[m.H.topLeftCorner(n,12) | 0(n x6 bias)]` (eskf.cpp:245-250) — GPS observes pose/twist, bias cols zero (correct). Same leak-free 6/18 padding as update() (rows [n,6) zero, R pad diag 1). `S = HPHᵀ+R`, NIS via one LDLT, gate (state unchanged on reject — early return before any mutation, :260), `K = PHᵀS⁻¹` (18x6) via `Sldlt.solve(HP)ᵀ` (same algebra verified correct in slice-11). Injection: pose `∘ exp(dx[0:6])`, twist `+= dx[6:12]`, **bias `+= dx[12:18]`** via the nonzero bias rows of K from the cross-cov (:272-274). Joseph 18x18 `(I-KH)P(I-KH)ᵀ+KRKᵀ` + symmetrize18 (:277-278) ⇒ PSD. Returns false when `!bias_active_` (caller routes correctly).
ENABLE_BIAS SEEDING — VERIFIED. `cov18_` zeroed, top-left 12x12 = live `state_.cov`, bias block `bias_cov0·I` (guarded >0, else 1e-6), cross-blocks 0, symmetrized (eskf.cpp:62-75). Starting cross=0 is CORRECT (no coupling accumulated at enable time; the predict builds it). `bias_prior_trace_` captured for confidence. PSD (Zero + PSD diag blocks). `kBiasCov0=1.0` ⇒ prior trace 6 — observable but not dominating. Enabled lazily once (`if (!bias_active())`) on the first drives-alone step (estimator.cpp:1336-1337).
DEFAULT-OFF BYTE-IDENTITY — VERIFIED. No bias source ⇒ `bias_slot=-1` ⇒ `bias_drives_alone` false every step ⇒ `predict()`/`update()` verbatim, `cov18_`/`bias_` never touched, the bias snapshot block (estimator.cpp:1400) gated off. Snapshot loop sets `bias=Zero`, `bias_observable=0` (:1276-1277). Sim `body_twist_bias` skipped when `squaredNorm()==0` (synthetic_source.cpp:115) ⇒ byte-identical reported delta. test_bias.cpp case (c) proves two bias-OFF runs byte-equal across >50 fused records (pose.R/t/cov + bias + observable). Pre-existing 195 cases unchanged.
OBSERVABILITY SELF-TEST — VERIFIED genuine. No-ref ⇒ no correction registered ⇒ `update_aug` never called ⇒ bias stays exactly 0, `bias_confidence` reads 0 (P_bias only grows under RW ⇒ `1 - P/P_prior < 0` ⇒ clamped 0). Live: bias norm 0, observable 0. Would FAIL if bias injection were wrongly wired into predict_aug (no-ref nonzero bias). With-ref convergence is non-vacuous: asserts recovered≈planted on the two EXCITED DOF (loose tol — see NIT) AND sign AND `on_tail_err < 0.6·off_tail_err` AND `applied_total>10` AND `max_observable>0.1`. The unexcited DOF (recovered ~ -0.04..-0.005) are correctly NOT asserted as converged.
bias_observable SEMANTICS — VERIFIED honest. `clamp(1 - trace(P_bias)/trace(P_bias_prior), 0, 1)` (eskf.cpp:83-88), bounded [0,1], div guarded by `bias_prior_trace_ > 0`. Reads 0 no-ref (P grows), rose to 0.996 with fixes (P_bias shrank to ~0.4% of prior — a real determination signal). No divide-by-zero / sign issue.
MULTI-BIAS GUARD + CONFIG — VERIFIED. validate() counts `bias_states` sources, `>1 ⇒ InvalidConfig`; `bias_process_noise < 0 ⇒ OutOfRange` (estimator.cpp:1756-1763). `bias_process_noise` added to `config_hash` (:211) so a persisted blob under a different bias config is rejected. test_bias.cpp case (d) covers all three (2 bias ⇒ InvalidConfig, 1 ⇒ Ok, negative pn ⇒ OutOfRange). `add_source` records the single bias slot/id/pn (first match safe since validate caps at 1); clamps negative pn to 0.
STRICT CORE / DETERMINISM — VERIFIED. Mat18/Vec18 fixed-size (no heap); `cov18_` lives in Eskf (member), no post-init allocation; all paths return Status/bool/void, no new exceptions; Joseph + symmetrize keep PSD; double throughout. Routing bounded (n<=max_sources). Sim plant is a pure function of (seed,t0,t1) ⇒ deterministic (case (c) two runs byte-equal). Only the existing `<Eigen/Cholesky>` LDLT is reused.
ROUTING / ORDER OF OPERATIONS — VERIFIED. `bias_drives_alone = bias_slot>=0 && n==1 && med_slot[0]==bias_slot` (estimator.cpp:1333-1334) — exact 1-source-median regime. predict_aug runs in place of predict; the correction loop routes update_aug iff `use_aug && bias_active()` (:1380-1382), same Mahalanobis gate. Bias snapshot filled AFTER predict_aug+update_aug (:1400-1409) ⇒ reflects this step's de-bias + injection. Calibration observe() still votes pre-correction med.value (unchanged from slice-11).

TOTAL: 6 findings (0 CRITICAL, 0 MAJOR, 1 MINOR, 5 NIT) + 11 clean categories.
