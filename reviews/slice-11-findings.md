# Slice 11 review — absolute-ref correction path + Mahalanobis-gated ESKF update @ 593e693

Scope: read the full diff (`git show 593e693`) — `eskf.hpp`/`eskf.cpp` (NEW `update()`), `result.hpp` (`CorrectionDiag`), `estimator.cpp` (`add_correction` + the step() correction loop + `validate` chi2>0), `absolute_ref_source.hpp`/`.cpp` (NEW `SyntheticAbsoluteRef`), `rig.hpp`/`rig.cpp` (`add_correction`), `test_correction.cpp` (NEW), `test_eskf.cpp` (update unit cases), `test_validation.cpp` (NIS), the two CMakeLists — plus the math it relies on in context: `absolute_ref.hpp` (`Measurement`/`ICorrection`), `lie.cpp` (`se3::exp`/`log`/`adjoint`, `so3::left_jacobian`), `types.hpp` (`State`, no augmentation), `config.hpp` (`bias_states`/`mahalanobis_chi2`). Against DESIGN §5/§7/§8/§10, DECISIONS D4/D8/D21/D22, ISSUES Slice 11, HANDOFF §5/§6, reviews/slice-14-findings.md (assertion-proportionality precedent). Built + ran full gate GREEN: **175 cases / 11009 assertions, 0 failed** (ctest 1/1, 5.0s; cosmetic `vswhere.exe`/`LF→CRLF` lines ignored). Observed live: drift removal baseline 0.579 m -> corrected 0.195 m, 83 fixes applied; gate 77 applied / 6 rejected, max pose err in outlier window 0.074 m, max rejected NIS 303774; NIS ensemble-mean 2.39865 for DOF=3 (consistent=NO). VERDICTS: (a) the ESKF update math (S, gain via ldlt, dx, Joseph) AND the injection tangent (`T ∘ se3::exp(dx[0..5])`, twist additive) are CORRECT and match predict()'s right-error full-SE(3) model — independently re-derived + numerically verified the 6/12 padding contributes EXACTLY zero to the active block; (b) the single-scalar Mahalanobis gate is a real-but-MINOR documented limitation (not a correctness defect), and the drift-test `chi2=100` is a SYMPTOM of the Slice-14 covariance pessimism, not a fresh defect — but the test's own framing under-states that the loose gate is masking the mis-calibrated P; (c) the bias-states deferral was HONORED (core `State` untouched, `bias_states` a no-op).

include/ofc/core/eskf.hpp:75-78 / src/core/eskf.cpp:126 / tests/test_correction.cpp:144-152: MINOR - the Mahalanobis gate is a SINGLE SCALAR `cfg.mahalanobis_chi2` applied REGARDLESS of measurement DOF `n` (the flagged smell — CONFIRMED, and the implementer documented it in eskf.hpp:75-78). `chi2=9` is ~the 97% quantile for 3 DOF but only ~80% for 6 DOF, so the same threshold over-gates a 6-DOF pose fix and under-gates a 3-DOF position fix. With only `dim=3` fixes shipping this slice it is harmless TODAY, but it becomes a real gating bug the moment a 6-DOF (pose) or mixed-DOF plugin lands. Not a correctness defect of the update math; a tuning/contract gap. Fix: gate on the per-`n` chi-square quantile (a tiny 6-entry const table indexed by `m.dim`, or pass the quantile per measurement) instead of one global scalar; until then keep the eskf.hpp:75-78 limitation note and add the same caveat to CONFIG.md's `mahalanobis_chi2` row.

tests/test_correction.cpp:144-152,181: MINOR - the drift-removal test loosens the gate to `chi2=100` to admit legitimate accumulated-drift residuals (a few tenths of a metre under a tight `sigma_pos=0.05` -> `S≈R` tiny -> NIS far exceeds 9). The comment frames this as a gate/noise-trust tuning, but the ROOT CAUSE is the Slice-14 covariance pessimism: a CORRECTLY-calibrated filter would let P grow between fixes so the drift residual's NIS stays ~`n` and `chi2=9` would already admit it — i.e. `chi2=100` is a SYMPTOM of the mis-calibrated P (init `P=I₁₂`, no shrink on the predict-only stretches), not an intrinsic property of "drift is large". This is the SAME pessimism the NIS case half-closes (mean 2.4 vs 3, biased low because P re-inflates between fixes). The test is not rigged — the comparison is non-vacuous and the gate case independently proves a 30 m/NIS≈3e5 outlier is still rejected at the SAME loose threshold — but the justification should name the mis-calibrated covariance as the real cause (and that a future smaller-init-P / denser-fix fix would let the default `chi2=9` work), so a reader does not take `chi2=100` as a recommended production value. Cross-ref reviews/slice-14-findings.md (NEES ≈ 0.13, ~46× pessimistic).

tests/test_correction.cpp:280-288 / tests/test_validation.cpp:663-694: NIT - assertion proportionality (the Slice-14 precedent, as flagged). The no-correction-identity case (test_correction.cpp:274-291) emits ~5 `CHECK`s per fused record (one folded byte-equality + 4 zero-default checks) across >50 records, and the NIS/validation suite re-runs the M=30 ensembles — together a large share of the 5809->11009 jump. Per-record byte-equality is already folded into ONE boolean (good — matches the Slice-14 fix at test_validation.cpp:670-694), but the 4 separate `corr_*==0` / `last_nis==0.0` checks per record (test_correction.cpp:285-288) re-assert the same "no-correction summary is zero" property hundreds of times. Collapse them into the same per-record fold (`ra.correction.corr_evaluated==0 && ...`) so the assertion budget stays proportionate to the property tested. Not a correctness issue (all pass) and the suite stays fast (5.0s); purely count hygiene.

src/core/estimator.cpp:950-961: NIT - `cdiag.last_nis` is overwritten every correction iteration, so with MULTIPLE registered plugins it holds the NIS of the LAST one EVALUATED, not the last one APPLIED (and `corr_applied + corr_rejected == corr_evaluated` only because `evaluate()` true always routes to exactly one of the two — which is correct). result.hpp:98-100 documents it as "the NIS of the LAST update() this step, accepted OR rejected", so the code matches the contract; but with N>1 plugins the single scalar is ambiguous for downstream NIS consistency (which plugin's innovation is it?). Single-plugin sims are unaffected. If multi-plugin NIS diagnostics are ever needed, make `last_nis` per-correction (or document that it is only meaningful for the 0..1-plugin case). Note only.

ISSUES.md:91-96: MINOR - DOC DRIFT (note, do not fix): the Slice-11 "Done when" has two halves — "(1) sim with GPS fixes removes pose drift" (DONE) and "(2) with `bias_states` on, a raw-IMU source's bias is observed and removed" (DEFERRED to Slice 11b per the brief). The issue still reads as one undivided slice with the `[ ]` box and lists "Optional per-source bias states; GPS adapter (in adapters/)" as in-scope. The orchestrator should split ISSUES Slice 11 into 11 (correction path, this commit) and 11b (bias states + GPS adapter) so the tracker reflects the approved scope split; otherwise Slice 11 reads as incomplete against its own done-criteria. DESIGN §5:89 and DECISIONS D22:109-110 (the optional-bias paragraph) likewise still describe bias states as part of D22 with no "deferred to 11b" marker.

sim/include/ofc_sim/absolute_ref_source.hpp:64-65 / sim/src/absolute_ref_source.cpp:42-47: NIT - the ref's GT anchor is correct ONLY if `AbsoluteRefParams::window_s` exactly equals the rig/estimator `Config::window_s` AND the first `evaluate()` lands on the rig's first fused frontier (both true in the tests, which set `rp.window_s = cfg.window_s` and emit on the first eligible frontier). If a caller forgets to mirror `window_s`, the anchor silently shifts the reference origin and the residual is biased by a constant offset (the drift test would still partially improve, masking the misconfig). This is a relaxed-edge sim fixture, not core, so it is a NIT, but a one-line `assert`/comment that `window_s` MUST match the rig is cheap insurance against a silent-bias footgun. The lazy `mutable` anchor is race-free (estimator pumps step() sequentially; single evaluate path).

CLEAN categories (verified, no finding):
ESKF UPDATE MATH (TOP PRIORITY) — VERIFIED CORRECT. `S = H P Hᵀ + R` (eskf.cpp:117); NIS `d2 = rᵀS⁻¹r` via `S.ldlt().solve(r)` (eskf.cpp:120-122, stable, no explicit inverse); gain `K = P Hᵀ S⁻¹` computed as `Kᵀ = S⁻¹(H P)` then transposed (eskf.cpp:130-132) — algebraically `Kᵀ = S⁻ᵀHPᵀ = S⁻¹HP` since S,P symmetric, so `Sldlt.solve(HP)` is exactly `Kᵀ` (CORRECT); `dx = K r` (:135); Joseph `(I-KH)P(I-KH)ᵀ + KRKᵀ` (:150-151) — the PSD-preserving form, then `symmetrize`. No fragile explicit inverse anywhere; LDLT reused for NIS, gain, all from one factorization.
ERROR-INJECTION TANGENT — VERIFIED, matches predict(). Pose `T <- T ∘ se3::exp(dx[0..5])` (eskf.cpp:143, full SE(3) exp of the `[trans;rot]` tangent — `se3::exp` uses `left_jacobian`, lie.cpp:97-104), twist additive `xi += dx[6..11]` (:144). This is the SAME right-error model predict() propagates (`T_true = T ∘ exp(eta)`, F = `Ad(delta⁻¹)`, eskf.cpp:7-13,67) and the SAME full coupled SE(3) tangent the Slice-14 NEES uses — NOT a left/right or decoupled/coupled mismatch. The feared inconsistency does NOT exist.
6/12 PADDING IS LEAK-FREE — VERIFIED NUMERICALLY. Unused H rows n..5 = 0, residual n..5 = 0, R diagonal padded with 1 (eskf.cpp:107-112). Independently re-derived + checked in numpy (random SPD P, n=3): the padded-6/12 `dx`, posterior `P`, and `d2` match the pure-n-dim update to machine precision (dx diff 1e-16, P diff 5e-15, d2 identical); the active<->pad off-diagonal blocks of S are EXACTLY zero (H pad rows zero => HPHᵀ pad rows/cols vanish, leaving R's independent 1×1 identity blocks). S stays invertible; padding contributes nothing to the active block and cannot singularize.
SIM H = [R|0|0] CONVENTION — VERIFIED. `h(x)=x.pose.t`; under `T∘exp(eta)`, `t(eta)=t+R·J_l(θ)·rho ≈ t+R·rho` => `dh/d(rho)=R`, `dh/d(θ)=0` to first order, so `H=[R|0|0]`, R=x.pose.R (absolute_ref_source.cpp:75-77). The discriminating unit case (test_eskf.cpp:339-355, 1.2 rad yaw so R≠I) collapses the residual to <25% ONLY if H uses pose.R, and the NIS-≈-DOF result (2.4 for 3 DOF) is consistent with H being right (a wrong H would bias S/NIS). Anchor `GT(first_frontier - window_s)⁻¹` (absolute_ref_source.cpp:45) exactly mirrors rig.cpp:69; GT expressed in the same odom frame the estimator anchors at the first fused tick.
ESTIMATOR WIRING — VERIFIED. `add_correction`: NotInitialized / null->InvalidConfig / over-cap (8)->CapacityExceeded (estimator.cpp:575-582); fixed-capacity array reset + nulled in init (:487-489). step() correction loop runs AFTER predict (:929) and BEFORE publishing frontier/tip (:967,:981); frontier + tip re-read from the CORRECTED eskf state (:953,:967,:981 — tip extrapolates from the corrected state). No heap (stack `Measurement m`, fixed-size Eigen), bounded by `correction_count<=8`, all paths return Status / bool. Corrections-off path is byte-identical (test_correction.cpp case (c), live: byte-equal + all-zero CorrectionDiag).
ORDER OF OPERATIONS (predict -> corrections -> publish -> calib feedback) — VERIFIED, does not break Slice-8. Calibration `observe()` (estimator.cpp:839,856) votes against `med.value` (the PRE-correction consensus median DELTA) and runs before the correction loop — so corrections never pollute the relative-motion calibration votes (intended: a calibration vote is about source-vs-consensus relative motion, not the absolute corrected pose). `apply_calib_feedback()` (:995) reads calibrator state (not eskf state) after publish, so the atomic between-step prior swap is untouched by corrections. Calibration sees the pre-correction consensus — correct and intentional.
STRICT CORE / SCOPE — VERIFIED. No post-init heap (corrections array fixed-cap 8; Measurement stack). `#include <Eigen/Cholesky>` for the LDLT is the only new core include (eskf.cpp:23), appropriate. Core `State` (types.hpp:42-47) NOT augmented — no bias states; `bias_states` flag (config.hpp:44) has ZERO references in src/ (grep clean) so it is genuinely a no-op; `State`/12-dim covariance unchanged. Bias-states deferral to Slice 11b HONORED.
MAHALANOBIS GATE SEMANTICS — VERIFIED (the single-scalar caveat is the MINOR above). Gating on `d2 = rᵀS⁻¹r > threshold` is the correct NIS test; rejected updates leave state byte-unchanged (early `return false` before any mutation, eskf.cpp:126) and still record `last_nis` (:123) — proven by test_eskf.cpp:297-319 (50 m outlier, NIS>9, pose/cov/twist byte-equal, last_nis>9) and the rig gate case (NIS 3e5 rejected, pose protected to 0.074 m).
NIS CHECK HONESTY — VERIFIED non-vacuous + honest. Ensemble-mean NIS 2.39865 vs tight chi² 99% interval [2.910, 3.091] for DOF=3 (live), `consistent=NO`, the ~20% shortfall attributed to P re-inflating between fixes (S biased high) — the mis-calibration is surfaced, not faked. Achievable band `(1.5, 3.5)` (test_validation.cpp:536-537) is a real regression guard (~1.5× drift breaks it) that brackets the observed 2.4, explicitly not the consistency interval. Tuning the sim `sigma_pos` to equal the drawn noise sigma is legitimate (it makes the filter's R match the actual draw — the only honest way to expect NIS~DOF). N=4860>1000 keeps the band non-degenerate.
DRIFT-REMOVAL TEST GENUINENESS — VERIFIED (the chi2=100 framing is the MINOR above). Non-vacuous: baseline genuinely drifts (live 0.579 m from a planted 4% scale held off the prior via `commit_min_votes=1e9`), corrected 0.195 m (<0.5× baseline, the assertion); the improvement is attributable to the correction (same planted params/config, the ONLY difference is the registered ref + the chi2 widening, which only admits MORE legit fixes). `applied_total=83>10` proves the path ran. Gate-rejection case is a real outlier rejection (priors==planted track GT, a leaked 30 m outlier would blow past the 1.0 m guard; observed 0.074 m).
DETERMINISM — PRESERVED. No-correction runs byte-identical (test_correction.cpp case (c), live pass); the ref's noise is a pure function of `(seed, stamp)` (`mix_seed`, absolute_ref_source.cpp:18-25) so replay is stable regardless of tick alignment (mirrors SyntheticSource). Validation golden determinism (with calib snapshot) still passes.
VALIDATE — VERIFIED. `mahalanobis_chi2 <= 0` -> OutOfRange (estimator.cpp:1067) with the correct rationale (a non-positive threshold rejects every measurement since NIS>=0). No regression to the other bounds.

TOTAL: 6 findings (0 CRITICAL, 0 MAJOR, 3 MINOR, 3 NIT) + 11 clean categories.
