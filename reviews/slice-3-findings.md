# Slice 3 review — lifecycle & degrade-don't-block @ a74ead8

Scope: read the full diff (`config.hpp`, `result.hpp`, `estimator.cpp`, `tests/CMakeLists.txt`, `tests/test_lifecycle.cpp`) + the WHOLE `estimator.cpp` step()/init()/validate()/Impl in context; against DESIGN §7 (Lifecycle bullet) + §8 (Output fields), DECISIONS D6/D23, ISSUES "Slice 3" done-criteria, CONFIG §2/§3, HANDOFF §1/§5. Rebuilt clean (touched estimator.cpp + test_lifecycle.cpp -> `[4/4] Linking ofc_tests.exe`, no errors) and ran the gate: 156 doctest cases / 5238 assertions, 0 failed; ctest 1/1 pass. Lifecycle subset alone: 8 cases / 78 assertions, all green. Phase ladder + readiness mapping traced by hand against every branch.

tests/test_lifecycle.cpp:181: MAJOR - the case titled "WARMUP on a non-causal interval (t1 <= q0)" never produces `t1 <= q0`, so the `t1 <= q0` early-return branch (estimator.cpp:546-550) is UNTESTED by the whole suite. With `now=0`, `fusion_delay_s=0.05`, `window_s=0.10` (first fuse, no prior frontier): `t1 = -0.05e9`, `q0 = t1 - window = -0.15e9`, so `t1 > q0` — the guard is skipped and the case actually exits via the `n==0` path (the source buffer never covers the negative interval). On the first fuse `t1<=q0` is algebraically impossible (`t1 <= t1 - window` ⟺ `window <= 0`, blocked by validate); it is reachable ONLY in steady state with a non-advancing/backward clock. Add a case that fuses once (advances `last_t1`) then steps with `now` <= the previous frontier so `t1 <= last_t1 = q0`, and assert NotReady + Degraded (ever_fused) / tip_valid==false — this is the only way to pin the degenerate-interval Degraded transition.

tests/test_lifecycle.cpp:136: MINOR - the case title "WARMUP before the first fuse (degenerate interval)" is misleading: as its own inline comment admits, it hits the `n==0` (no covering source) path, NOT the degenerate-interval guard. Rename to "...(no covering source)" so the two WARMUP entry points are not conflated (the genuine degenerate-interval path is the MAJOR above).

tests/test_lifecycle.cpp:359: MINOR - the anchor-at-first-tick translation assertion (`f.pose.t.norm() < 0.35`) does not actually discriminate "anchored at first tick" from "pre-integrated from t=0": on the FIRST fuse `q0 = t1 - window_s = 0`, so an anchored integrator and a from-zero integrator integrate the identical interval [0, 0.10] -> ~0.20 m, both under the bound. The rotation==identity check is likewise true for any straight-segment integration. The test proves "frontier reflects ~one window of forward motion", not the gauge anchor specifically. Strengthen by asserting the published frontier stamp == t1 AND `t.norm() ≈ 2.0*dt` within tol (an equality, not a loose ceiling), or fuse twice and check the first frontier's displacement is exactly one window (not two).

src/core/estimator.cpp:213: MINOR - `publish_phase()`'s switch has no `default` and the enum is closed today, so all four cases set `readiness`; but a future `Phase` value would silently leave `readiness` at its last value with no compiler diagnostic. Add `default:` (or a static_assert on the enum count) so the phase->readiness map stays total. Note: with `-Werror=switch` this is already caught at compile time — confirm the build enables it; if not, this is a latent drift risk.

src/core/estimator.cpp:807 / DESIGN.md:113: MINOR - NOMINAL is purely source-count driven (`n >= min_sources_warn`), ignoring calibration convergence, whereas DESIGN §7/D23 say "auto-upgrade as calibration converges". Under ReferenceOnly the upgrade IS indirectly coupled (a source joins the median — raising `n` — only once `ext_committed`, see participates()), so DEGRADED->NOMINAL does track convergence there; but under MedianFromStart `n` is just the covered-source count and NOMINAL says nothing about whether extrinsics have converged. Acceptable for Slice-3 scope (per-DOF confidence is exposed via CalibSnapshot), but the readiness/phase ladder does not itself encode convergence — flag as a spec-vs-impl scope gap, not a bug.

CONFIG.md:37: MINOR - DOC DRIFT (note, do not fix): CONFIG §3 documents `min_sources_warn` range as `[1, max_sources]`, but validate() intentionally enforces lower-bound only and the code/test treat `> max_sources` as legitimate ("never NOMINAL"). The doc's upper bound contradicts the implemented + tested contract. Orchestrator should reconcile CONFIG §3 to `[1, ∞)` (or document the upper bound as advisory).

HANDOFF.md:99: NIT - DOC DRIFT (note, do not fix): HANDOFF §6 still lists "Not yet built: lifecycle (3)" though Slice 3 is now implemented; orchestrator owns this doc update.

tests/test_lifecycle.cpp:285: NIT - in the graceful-downgrade case, Phase B only requires `saw_degraded_ok` and that the run ENDS Degraded; it does not pin WHEN the downgrade occurs relative to the short sources dropping out (the loop tolerates Nominal OR Degraded on every step). The non-vacuous proof of "retained last frontier" is in Phase C and is solid (1e-12 equality on t and R). Acceptable, but the downgrade-trigger coupling (n drops below warn the step the short sources leave the window) is asserted only loosely.

CLEAN categories (verified, no finding):
Readiness/phase coherence — every phase write in step() and init() routes through `publish_phase()` (estimator.cpp:440,547,665,807), which sets `result.phase` AND `result.readiness` together from one Phase; the two fields cannot drift in ANY branch (both early returns + the fuse path). Map matches the brief exactly: Init 0.0 / Warmup 0.25 / Degraded 0.6 / Nominal 1.0, monotone.
Ladder correctness — INIT set in init() before any step and observable via latest() (test :81); never re-entered after init (no code path writes Init in step()). WARMUP only pre-first-fuse (`!ever_fused_`). `ever_fused_` latches true only on the successful fuse path (estimator.cpp:806) and is reset in init() (:439); it correctly distinguishes pre-fuse WARMUP from post-fuse DEGRADED on total source loss. Fuse path: `n >= min_sources_warn -> Nominal else Degraded`. No transition can thrash within a step (phase recomputed once, deterministically, from `n`).
Degrade-don't-block — `n==0`-after-fuse returns NotReady + Degraded WITHOUT writing `result.frontier` (estimator.cpp:664-668), so the last good frontier is retained (proven at test :322-323, 1e-12). Reference-only dead-reckon emits Ok as early as the reference covers (test :255-263, n==1 -> Degraded+Ok+valid tip). Graceful NOMINAL->DEGRADED on partial loss and ->DEGRADED+NotReady on total loss both exercised (test :290-323).
Strict core / non-regression — the two new Impl members are a `Phase` enum + a `bool` (heap-free, fixed); both reset in init() (:439-440). `publish_phase()` is branch-only (no heap, no alloc, bounded). validate() adds one lower-bound compare (no throw, Status return). step() still allocates nothing. `min_sources_warn` default 3 did not regress any existing assertion — full suite green (5238/5238). All returns are Status; no exceptions; loops still bounded.
validate() bound — `min_sources_warn < 1 -> OutOfRange`; not tied to max_sources (test :366-376 pins 0=>OutOfRange, 1=>Ok, 99=>Ok). Matches the brief's "lower-bound only" intent.
Gauge anchor — `eskf.init(SE3{})` on first fuse (estimator.cpp:786-789) anchors the odom frame at the first fused tick (D23 / DESIGN §7); frontier stamp driven from real t1 (:799). (Test coverage of this is weak — see the :359 MINOR — but the implementation is correct.)
Test determinism / helper consistency — all timestamps come from `secs()` (deterministic ns), no wall-clock anywhere. Copied helpers (`secs`/`close`/`BufferSource`/`gt_twist`/`fill_gt`) are byte-consistent with test_fusion.cpp; `base_config()` adds an explicit `cold_start` param defaulting to MedianFromStart, matching test_fusion's hardcoded MedianFromStart. CMakeLists wires test_lifecycle.cpp into ofc_tests.

TOTAL: 8 findings (0 CRITICAL, 1 MAJOR, 5 MINOR, 2 NIT) + 7 clean categories.
