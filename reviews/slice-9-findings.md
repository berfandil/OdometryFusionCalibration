# Slice 9 review — variance-EMA reliability weight refinement @ e7404d5

Scope: read the full diff (`config.hpp`, `result.hpp`, `estimator.cpp`, `tests/CMakeLists.txt`, `tests/test_weights.cpp`) + the WHOLE `estimator.cpp` step()/init()/validate()/Impl in context + `src/core/lie.cpp` `split_distance`; against DESIGN §4 (Weights/Reliability bullets), DECISIONS D17 + the D21 `sigma_confidence` impl note, ISSUES "Slice 9" done-criteria, CONFIG §4, HANDOFF §1/§5/§6. Clean rebuild from scratch (`[29/29] Linking ofc_tests.exe`, no errors) + full gate green: 161 cases / 5317 assertions, 0 failed; ctest 1/1 pass (cosmetic `vswhere.exe`/`LF→CRLF` lines ignored). Weights subset alone: 5 cases / 70 assertions, all green. EMA-variance recurrence, causal ordering, median-of-variances baseline, and the bias/variance split traced by hand.

tests/test_weights.cpp:635-648: MINOR - the biased-source case's HEADLINE assertion (c) `cs->scale ≈ 1.20` + `scale_confidence > 0` is NON-VACUOUS as a convergence check but is NOT discriminating for the D17 reliability/bias SPLIT (the property under review). The scale calibrator votes are weighted by `s.calib_conf[nc] = sigma_conf` (estimator.cpp:694), which is INDEPENDENT of the reliability track; every covered source feeds `calib1` regardless of its fusion weight (estimator.cpp:692-695, the `nc` loop is outside `participates()`). So scale would still converge even if the bias HAD collapsed the biased source's reliability — (c) cannot fail on a broken split. The only assertion that actually pins the split is (a) at :632-633 (`hb->reliability > 0.7*clean_rel` and `> floor + 0.2`); it IS discriminating (a bias-inflates-variance regression floors reliability → (a) fails). Re-label (c) in the comment as corroborating-not-discriminating, or strengthen it to compare against an ablation where the split is broken; do not present it as the headline split evidence (the brief's deviation (b) rationale overstates its discriminating power).

include/ofc/core/result.hpp:74 / src/core/estimator.cpp:743,755,800: MINOR - `SourceHealth::bias` is documented as "routed to the CALIBRATOR" but `resid_mean` is ONLY surfaced as a diagnostic (h.bias) — it is not wired into `calib1`/`calib2` anywhere. The systematic component IS in fact acted on by the calibrator, but via the pre-existing Slice-6/7/8 observe path (the calibrators independently see the consensus disagreement in `B_corr`), NOT via this `bias` field. The D17 "bias → calibrator, not weight" contract is met (the routing is architectural; Slice 9's job is to NOT fold bias into the weight, which it does), but the comment reads as if `bias` is an active feedback wire. Judgement: sufficient per D17; reword the doc to "the systematic component is left for the calibrator to absorb (it is exposed here for diagnostics only; the active routing is the existing calibrator observe path), and is deliberately NOT folded into the weight."

src/core/estimator.cpp:743,800 / include/ofc/core/result.hpp:73: NIT - `bias` (= EMA mean of `d = split_distance`) is an UNSIGNED residual MAGNITUDE (split_distance returns `sqrt(...) ≥ 0`, lie.cpp:135), not a signed systematic offset. A source biased high and a source biased low in any DOF both read a positive `bias`, and even a zero-mean noisy source accrues a positive `bias` (mean of |residual| > 0). So `bias` is "mean residual distance to consensus", which conflates magnitude-of-disagreement with direction-of-disagreement; it cannot by itself distinguish a systematic offset from noise (that is exactly what `resid_var` is for). Harmless for the weight (which only uses `resid_var`) and for the biased test (which compares magnitudes), but the field name/doc imply a signed bias it does not carry. Note the semantics in the doc.

CONFIG.md:42-50: MINOR - DOC DRIFT (note, do not fix): CONFIG §4 "Source weights" table does not list the two new knobs `reliability_floor` (default 0.2, range (0,1]) and `reliability_cap` (default 5.0, range [1,∞)) added in config.hpp:52-53. Orchestrator should add both rows.

HANDOFF.md:100 / ISSUES.md:79: NIT - DOC DRIFT (note, do not fix): HANDOFF §6 still lists "weight-refine (9)" under "Not yet built", and ISSUES "Slice 9" header is still `[ ]` though this commit implements it. Orchestrator owns these doc/checkbox updates.

CLEAN categories (verified, no finding):
Bias/variance split correctness — `resid_var` is the EMA variance of `d` AROUND the running mean: `delta = d - resid_mean` (BEFORE the mean update), `resid_var += alpha*(delta² - resid_var)` (estimator.cpp:754-756). A CONSTANT-bias source's `delta → 0` once the mean converges, so its bias does NOT inflate the variance; with equal common noise its `resid_var ≈ the clean baseline → ratio ≈ 1` (the D17 property). resid_mean = the bias. Verified the biased test rig (scale 1.2 + shared 0.003 floor) keeps the biased source's scatter ≈ the clean scatter.
EMA numerical stability — the West recurrence is a convex combination `resid_var_new = (1-alpha)·resid_var + alpha·delta²` with `alpha ∈ (0,1]`, `resid_var₀ = 0`, `delta² ≥ 0` ⇒ `resid_var ≥ 0` for all steps, monotone-bounded by max delta²; it can NEVER go negative or NaN on finite inputs (the `delta²-resid_var` form does not subtract an unbounded quantity). resid_mean is a plain EMA, bounded by the d range. d itself is `sqrt(...) ≥ 0` finite (lie.cpp:132-136).
Causality / ordering — the weight at step k reads `s.reliability[i]` (estimator.cpp:686), which was last written either at init (1.0) or at the END of a PRIOR step's update block (:790); the EMA + reliability recompute run strictly AFTER `median::solve` (:727) and after the weights are formed (:740-792). No circularity: this step's residual never weights this step's median. Verified by construction (the update block is below the median, the weight is above it).
Cold-start / non-participant handling — the EMA + reliability loops iterate `med_slot[0..n)` = THIS step's median participants only (:740,767,786,796); a covered-but-excluded source (ReferenceOnly pre-commit) is never touched, so it keeps its last `reliability`/`resid_*` and does NOT enter the median-of-variances baseline (the `q` scratch is filled from med_slot only). Per-step `h.reliability/h.bias` reset to neutral 1.0/0.0 for every slot at :661-662 before the participant re-publish, so a non-participant reads neutral, not stale. A source that drops out then returns resumes its EMA (resid_n persists) — intended (slow track).
Strict core — all new state is fixed `Scalar[kMaxSourcesCap]`/`int[kMaxSourcesCap]` in Impl (:222-228), zero/neutral-reset in init() (:495-498); no heap after init. The median-of-variances scratch `rel_scratch` is a fixed STACK array sized at the cap, filled bounded by n and median-selected in place; `std::nth_element` on it is heap-free + bounded WCET (it is NEW to src/core but operates on the fixed array, so acceptable — not a vector, not a sort). All three new loops are bounded by `n ≤ max_sources ≤ kMaxSourcesCap`. validate() adds two range compares (Status return, no throw). All returns Status; double throughout.
Median-of-variances baseline — `ref_var` = lower-middle of the warmed-up participants' `resid_var` (`mid = q/2`, deterministic even-q tie-break, :775-776); with 1 noisy + 3 clean it robustly selects a clean variance (the noisy outlier sorts to the top, excluded from the median) → clean ratios ≈ 1, noisy ratio < 1. The `q ≥ 2` gate holds prior reliability when < 2 warmed-up participants qualify (no thrash on short/sparse runs); pre-warmup slots (`resid_n < kRelWarmup`) are skipped in both the baseline fill and the per-source recompute (:769,788), so reliability stays exactly 1.0 → Slice-2 weights intact before warmup.
Deviation (a) — numerator floor `max(ref_var, eps)`: identical to the spec `ref_var/max(resid_var,eps)` whenever `ref_var ≫ eps` (any rig with real noise — `max(ref_var,eps) == ref_var`), so it does NOT distort the intended downweighting when variances are non-tiny (a noisy source's large resid_var still drives ratio < 1 → floored). It ONLY differs in the degenerate near-zero-variance regime, where it correctly yields ratio ≈ eps/eps = 1 (reliability ≈ 1) instead of an ill-conditioned 0/0 collapsing every clean source to the floor. The equal-noise → reliability ≈ 1 invariant is preserved (equal resid_var → ratio = 1 regardless of the floor). Legitimate, property-preserving.
Deviation (b) — asserting the converged scale ESTIMATE over the scale_committed flag: legitimate given the all-4-sources-noisy rig's vote starvation of the shared Phase-1 straight regime (a hard commit latch IS unreliable here, per Slice-8's commit_min_votes mass-threshold caveat). The convergence assertion is non-vacuous (real movement off the 1.0 prior to ≈1.2). See the MINOR above for the one caveat: convergence is decoupled from the reliability track, so it corroborates but does not DISCRIMINATE the split — assertion (a) carries that load.
sigma_confidence unchanged — `sigma_confidence()` (estimator.cpp:81-85) is byte-identical to pre-Slice-9; its unit-mixing placeholder comment is intact (:76-80). Reliability is added MULTIPLICATIVELY as a separate factor (`w = prior · reliability · sigma_conf`, :686), exactly DESIGN §4 / D17 `w = prior × reliability × Σ-confidence`. The D21 note's "sigma_confidence unit-mixing until Slice 9 reliability EMA" is satisfied by ADDING the reliability quality factor (the placeholder is now backed by the new track), not by rewriting sigma_confidence — consistent with the implementer brief.
Non-regression / determinism — reliability holds at 1.0 pre-warmup and ≈1.0 for equal-noise clean sources (median-of-variances → ratio ≈ 1), so clean-rig fusion is unperturbed (full suite green, 5317/5317, no tolerance change in any prior test). `nth_element` + the EMA are pure functions of the (deterministic, seeded) inputs; `rel_scratch` is per-step scratch with no cross-step leak, so replay is byte-stable — the determinism case (:654-686) pins reliability+bias bit-identical across two runs.
validate() bounds — `reliability_floor ≤ 0 || > 1 → OutOfRange`; `reliability_cap < 1 → OutOfRange` (estimator.cpp:991-994), matching the (0,1] / [1,∞) spec; the test pins 0.0/1.5 floor and 0.5 cap as OutOfRange and the inclusive endpoints 1.0/1.0 as Ok (:420-437). Defaults (0.2/5.0) validate Ok.
Test quality (non-split cases) — pre-warmup reliability==1.0 is real (a genuine <20-fuse run, `REQUIRE(fuses < 20)`, :463). Noisy-downweight is non-vacuous + discriminating (widened weight clamp so reliability is VISIBLE in w; asserts `< 0.8×` clean on BOTH reliability and weight, `≥ floor`, clean ≈ 1.0 — would fail if bias inflated variance OR if the floor/cap were mis-wired). Determinism is genuine (bit-equality on the full reliability+bias vector). No case asserts something trivially true.

TOTAL: 5 findings (0 CRITICAL, 0 MAJOR, 3 MINOR, 2 NIT) + 12 clean categories.
