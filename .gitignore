# Build
/build/
/out/
*.o
*.a
*.so
*.obj
*.exe

# CMake
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
compile_commands.json

# Editor / OS
.vscode/
.idea/
*.swp
.DS_Store

# Claude local settings
.claude/settings.local.json

# Persistence snapshots (runtime artifacts)
*.ofc-state

# FetchContent offline cache (local dev workaround; not source)
/.deps_cache/

# Real-dataset replay working dirs (derived CSVs + manifests + results; not source)
/kitti_run/
/kaist_run/
/euroc_run/
/nuscenes_run/
