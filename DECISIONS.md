# Design Decision Log

Record of the design-grilling session: each decision, the option chosen, the alternatives rejected, and the rationale (including the catches that grilling surfaced). The resulting specification lives in [`DESIGN.md`](./DESIGN.md); this file is the *why*.

Scope chosen up front: grill the **whole system architecture**, greenfield.

---

## D1 — Motion domain → **platform-agnostic core**
- **Chosen**: abstract manifold + plugins from day one.
- **Rejected**: SE(3) ground robot (my rec), SE(2) planar, aerial 6-DOF.
- **Why / resolution**: agnostic + lightweight only reconcile as **small generic core + thin platform plugins** — generality at the edges, never the center. Load-bearing for everything below.

## D2 — Fusion engine → **ESKF on Lie groups**
- **Chosen**: error-state KF, calibration handling decided separately later.
- **Rejected**: IEKF, fixed-lag smoother, full incremental smoother (GTSAM).
- **Why**: bounded compute/memory honors "lightweight"; error-state keeps manifold math clean. Accept weaker calibration observability vs a smoother; mitigate elsewhere.

## D3 — Propagation input → **weighted geometric median of all odometries** (user's design)
- **Chosen**: buffer each source, integrate over a common window to an SE(3) delta, collapse to one consensus via weighted **geometric median in SE(3)**.
- **Corrections made**: (a) "median" requires the **unsquared** distance (Weiszfeld IRLS) — squaring gives the Karcher *mean* and loses robustness; (b) SE(3) has **no bi-invariant metric** → use a **split SO(3)/ℝ³ metric**; (c) needs **≥3 sources** to reject an outlier.
- **Why**: robust consensus motion instead of trusting one source.
- **Impl note (median-solver PINNING bug + fix, `1142e41`)**: the n≥3 Weiszfeld solver originally INITIALIZED at the highest-weight input vertex. At iter-0 that vertex's split-distance to itself is 0, so its ε-regularized weight `w/eps ≈ 1e9` (eps=1e-9) UNCONDITIONALLY dominated the reweight → the solver "converged" in ONE iteration sitting on that vertex and **returned the highest-weight source's delta verbatim** — NOT a robust interior median. Consequences: fusion was effectively "track the highest-weight source, lightly outlier-guarded"; a HIGH-WEIGHT OUTLIER (a faulty high-confidence sensor) was returned with ZERO rejection (its own d=0 self-weight was immune to the 1/d reweight → ~0.58 m/window vs a true median's ~0.009 m); the existing outlier-rejection tests passed only by ACCIDENT (the outlier was placed at a non-start vertex). Confirmed by spikes + clean-build measurement. **Fix**: initialize OFF-vertex at the weighted mean (translation arithmetic mean + one Karcher rotation step about the highest-weight R) so every dᵢ>0 and the 1/d reweight engages, plus a **Vardi-Zhang coincident-vertex guard** (exclude any dᵢ≤eps self-term) as a safety net. Now a true interior robust median (rejects a high-weight minority outlier; per-window fused error ~1/n of single-source; high-weight-outlier guard added). WCET unchanged (max_iters=10 cap; the value is correct within the cap even though Weiszfeld's linear tail may not latch the `converged` flag). This bug had MASKED two latent calibration issues now surfaced (see ISSUES.md) and obsoleted the pinning-median covariance calibration (see D4).

## D4 — ESKF correction step → **robust integrator + optional absolute refs**
- **Chosen**: predict on median; **Q from inter-source spread** (adaptive); corrections only from optional absolute-reference plugins; no absolute ref ⇒ honest dead-reckoning.
- **Rejected**: fixed Q (wastes the spread signal); update from per-sensor KFs (double-counts → inconsistent covariance); const-vel predict with odom as updates (abandons median-drives-predict).
- **Why**: a KF without an update is an integrator — name it honestly; the spread gives adaptive Q for free.
- **Impl note (Slice 14 covariance calibration — CURRENT, `1142e41`)**: the predict-only no-ref covariance is calibrated by **`q_scale` alone** (`q_pose = q_scale·spread²·I6 + q_floor`), recalibrated against the **true interior median** (after the D3 pinning fix). `q_scale 0.7` is the safety-first choice: worst-case ensemble-mean pose NEES ~4.85 (1×) / ~3.9 (2×) across {nees_traj, mixed, turning, straight} × {1×,2× noise} — as close to DOF=6 as possible FROM BELOW while NEVER overconfident (the gap to 6 is a deliberate safety margin; the sim under-states real model mismatch). With the true median, SMALLER `q_scale` → LARGER NEES (the opposite of the old pinning-median sign). The earlier **init-P fix** (`70c7d38`: seed P=0 at the first fuse, first `predict()` establishes `blkdiag(q_pose, q_pose/dt²)`) still stands.
- **Impl note (Slice 14, approach A — REVERTED, `730bcfa` … removed in `1142e41`)**: a `/n_eff` divisor (`adaptive_q_source_reduction` knob) was added on the rationale "the geometric median of n agreeing sources has ~1/n variance, so divide Q by n_eff." That rationale was **FALSE** for the then-current solver: the median solver was PINNING (D3) and returned ONE source verbatim — it averaged NOTHING, so the fused variance equalled a single source's, not 1/n. The `/n_eff` was actually an empirical fudge compensating for the pinning median's INFLATED spread (distance-to-vertex). Once D3 fixed the median to a true interior blend, the spread became correctly sized (distance-to-centroid) and `/n_eff` OVER-divided Q → **overconfident** (NEES ~20). So `/n_eff` was **removed entirely** (knob + param gone); `q_scale` alone calibrates the covariance (above). Step 4's `q_scale=0.5` (`983fa65`, calibrated against the buggy pinning median) is likewise superseded by `0.7`. Lesson: steps 3–4 were calibrating around the D3 bug; fixing the bug obsoleted them and unlocked genuine near-consistency (NEES 2.07 pessimistic-by-design → ~4.85 near-consistent). A distance-aware *shape* model / NHC no-ref correction remain the path to strict per-trajectory consistency to DOF=6 (deferred). `q_floor` default kept small (GPS-gain trade is a deployment choice — CONFIG §3).

## D5 — Calibration regime gating → **symmetric gates** (+ user refinements)
- **Chosen**: Phase 1 gated straight (`‖ω‖<ε ∧ ‖t‖>δ`); Phase 2 gated turning (`‖ω‖>θ`), votes weighted by `‖ω‖`.
- **Catch (load-bearing)**: roll and the **xyz lever-arm are unobservable in straight motion** — the user's original Phase 2 had no turn-gate. Straight-motion samples give *unconstrained* (not wrong) xyz that smear the histogram.
- **User refinements**: reverse-direction handled explicitly; optional reference-sensor cross-check (ice-slide / drone niche); turn magnitude usable as vote weight.

## D6 — Gauge & bootstrap → **base frame + pinned reference sensor**
- **Chosen**: user base frame; one reference sensor pinned (extrinsic = prior) as the gauge; bootstrap all extrinsics from config priors and iterate; **cold-start switch** (reference-only-until-confident *or* median-from-start).
- **Rejected**: float the gauge (underdetermined, never settles); raw-frame median (breaks for non-aligned mounts).
- **Why**: odometry-only ⇒ **gauge freedom** — extrinsics recoverable only relative to a pinned anchor. The median needs frame-aligned deltas (needs extrinsics) while calibration needs the fused output → circular; bootstrap-from-prior breaks it.

## D7 — Source contract → **uniform delta query + native ⊕ modeled Σ** (+ user refinement)
- **Chosen**: `delta(t0,t1) → (SE3, Σ6×6)`; adapter converts twist/increment/absolute and interpolates in-window. Confidence = Σ.
- **User refinement**: combine the provider's **native** Σ with a **modeled** Σ (configurable); missing native → identity.
- **Why**: one "confidence" definition flows to median weight, adaptive Q, and histogram votes.
- **Impl note (Slice 1)**: missing native Σ is treated as **identity** then run through the configured combine rule (not modeled-only); native-present is decided by the explicit `native_confidence` flag, not by inspecting the matrix. Absolute-pose providers' supplied Σ is taken as the per-step increment covariance (no differencing of absolute covariances — not PSD-safe).

## D8 — ESKF update step (revisited with full picture) → **integrator + optional absolute**
- Confirmed D4 once calibration was settled as a separate subsystem.

## D9 — Histogram vote aging → **configurable decay / sliding-K** (user)
- **Chosen**: config between **exponential decay** and **sliding window of K**.
- **Why (forced)**: bootstrap means early votes are computed with *wrong* priors → systematically biased → must wash out. Also tracks re-mount/thermal drift. Pure accumulation would never forget the bias.

## D10 — Phase-2 cost → **hand-eye, split solve** + **pairwise** (user wants both, compared)
- **Chosen**: implement two strategies and compare: (1) per-sensor hand-eye `A·X=X·B` vs fused base — **roll = 1-D rotation residual, xyz = linear-LS** `(R_A−I)t_X = R_X t_B − t_A`; (2) **pairwise** hand-eye between sensors with the reference extrinsic **fixed**.
- **Key structure**: xyz is *linear* given rotation; roll is the only nonlinear DOF (1-D). The LS singularity at `R_A=I` independently re-derives the turn-gate.

## D11 — Direction histogram (pole problem) → **3-channel so(3)/tangent @ per-sensor prior** (user's idea, refined)
- **Discussion path**: rejected naive 2×1-D (yaw,pitch) — poles. Considered: 3-component Cartesian (anisotropic, ±1 boundary, renormalize); Fibonacci equal-area sphere; tangent-plane @ running estimate (recenter fights aging).
- **Chosen**: the so(3) rotation vector of the minimal rotation, histogrammed per channel, basepoint = **each sensor's prior** forward.
- **Insight**: this *is* the tangent-plane method with a **fixed** basepoint → kills the recenter con; data sits near the basepoint (small tilt, no pole/antipode, isotropic). **User correction**: store **all 3 channels** (φ_x≡0 only in the basepoint-aligned frame; noise makes it nonzero), take the so(3) mode, convert to yaw,pitch **at the end**.

## D12 — Output timing → **lagged frontier + predicted tip**
- **Chosen**: fixed-rate tick; fuse causally at `now − delay` (consistent state + Σ); also expose a predict-only extrapolation to `now` (inflated Σ) for control.
- **Rejected**: lagged-only (no real-time tip); OOSM (complex); fixed-lag re-smoothing (drifts toward rejected smoother).

## D13 — Output contract → **transport-agnostic library + adapters**
- **Chosen**: pure C++ core, no middleware dep; callback + poll; rich result struct (frontier state, tip, per-sensor calib snapshot, diagnostics). ROS/DDS/zmq adapters outside core. Pose in an odom frame anchored at init (drifts); extrinsics in base frame.
- **Rejected**: ROS-first (couples core); pose-only (hides calib/diagnostics); streaming IPC (transport dep).

- **Impl note (Slice 13, adapters subset)**: a relaxed-edge `adapters/` tree (`ofc_adapters`, links the core PUBLIC API only, no new deps; the gate builds+tests it via `OFC_BUILD_ADAPTERS=ON` in `dev.ps1`). Shipped: a **file-persistence** double-buffer ping-pong (production form of the Slice-12 core serialize/deserialize — overwrite target chosen by VALIDITY, not raw seq, so a torn higher-seq file can't clobber the last-good; a review CRITICAL was caught + fixed here), a **threading wrapper** (`ThreadedEstimator` — one worker pumps `step()`, mutex-guarded `Result` snapshot), and a dep-free **config loader** (D19). The **ROS node** is a compile-guarded header sketch only (no ROS on the dev box) and a true `fsync` is deferred — both → **Slice 13b**.

## D14 — Threading → **caller-pumped single-thread** (+ user: **C++14 / AUTOSAR**)
- **Chosen**: core is a state machine the consumer pumps via `step()`; fusion + bounded calibration slice per step; lock-free, deterministic, trivially testable. Threading = external adapter.
- **Constraint added by user**: **C++14** (AUTOSAR). Ripples: no `std::optional`/`variant`/`string_view`, likely no-exceptions, no runtime heap, bounded WCET, `double`/no-fast-math. Mostly *reinforces* prior choices.

## D15 — Memory/error policy → **strict core, relaxed edges**
- **Chosen**: core = no heap post-init, no exceptions, hand-rolled `Optional`/`Expected`, bounded loops, fixed-capacity from config; adapters + tests stay relaxed (off-target).
- **Rejected**: strict-everywhere (painful test ergonomics); pragmatic-harden-later (retrofit touches every file); dual-build (#ifdef drift).

## D16 — Time-sync → **xcorr of ‖ω‖ → offset histogram** (+ user: pluggable metric)
- **Chosen**: cross-correlate the **extrinsic-invariant** ‖ω‖(t) signal (so temporal decouples from spatial and runs first), parabolic sub-sample, excitation-gated, vote into a per-source offset histogram (reuse the robustness primitive). Constant offset + slow track.
- **User note**: make the **match metric pluggable** (L1, L2, ratio, NCC…) — worth sweeping.
- **Rejected**: continuous SSD (no histogram robustness); joint-with-extrinsics (breaks the decoupling); ω-vector correlation (needs extrinsics first).

## D17 — Weight refinement → **variance-EMA reliability, bias → calibration**
- **Chosen**: `w ∝ 1/σ̂²` from **zero-mean** residual scatter (slow, floored/capped); the **systematic** residual routed to the calibrator, not the weight; Weiszfeld `1/d` handles per-step outliers. Final `w = prior × reliability × Σ-confidence`.
- **Why**: downweighting a *systematically* disagreeing source masks a miscalibration the calibrator should fix, and risks majority lock-in suppressing a correct minority. Split bias from variance.
- **Impl note (Slice 9)**: per-source EMA mean (`resid_mean` = bias) + EMA variance around that mean (`resid_var` = zero-mean scatter) of the residual-to-consensus distance, at `reliability_ema_alpha`. `reliability = clamp(ref_var / resid_var, reliability_floor, reliability_cap)`, `ref_var` = robust median of warmed-up participants' variances (equal-noise → ≈1; noisier → <1; cleaner → >1, floored/capped). `w = clamp(prior × reliability × Σ-confidence, weight_floor, weight_cap)`. **Causal** (weight reads the prior-step reliability; the EMA updates after the median, so this step's residual never weights this step's median) and 1.0 until a per-source warmup. The bias is **not** folded into the weight — it is left for the existing Phase-1/2 calibrator observe path to absorb (the routing is architectural, not a new feedback wire); `SourceHealth.bias` is diagnostic-only and is an unsigned residual *magnitude* (mean `split_distance`), not a signed per-DOF offset. `sigma_confidence()`'s unit-mixing (D21) is left intact — reliability is an additive quality factor, not a rewrite of the Σ-confidence; the unit-separation caveat stays open.
- **Impl note (Slice 19b, per-channel under split)**: under `split_median=true` the residual feed is per-channel against the split consensus (`d_rot = ‖log(R_mᵀR_i)‖` rad, `d_trans = ‖t_i−t_m‖` m on the same aligned deltas the solver consumed) into two independent Welford-EMA tracks (shared `resid_n_split` count; same alpha/warmup/eps/floor/cap knobs — no new config), each channel with its own median-of-variances reference. Weights: `w_trans = clamp(prior·rel_trans·σconf)`, `w_rot = clamp(prior·rel_rot·σconf)·rot_weight_prior`. The mixed scalar EMA is FROZEN under split (its persisted state is inert; per-channel state is NOT persisted → re-warms ~`kRelWarmup` fused steps after a warm restart). `SourceHealth` gains `reliability_rot/trans` + `bias_rot/trans`; legacy `.reliability`/`.bias` mirror the TRANSLATION channel under split (coupled path: new fields stay at defaults). Coupled path byte-identical (exact-equality pin captured pre-change). **Known property**: a noisy source holding ≥ half a channel's mass self-validates (residual-to-own-consensus ≈ 0 → reliability caps) — scatter reliability cannot dissolve a majority lock-in; `rot_weight_prior` (layer a) / the veto stay the levers there. Commit `51b86bf`; review `reviews/slice-19b-findings.md` (APPROVE, no findings).

## D18 — Per-sensor KF → **CV twist ESKF (ℝ⁶)** (+ user: fixed-lag smoother)
- **Chosen**: per-sensor (config on/off) constant-velocity error-state KF on body twist, random-walk accel; smoothed twist + refined Σ.
- **User insight (load-bearing)**: calibration tolerates latency → run it as a **fixed-lag RTS smoother** (two-sided) at a **deeper frontier** (`now − delay − L`). Two-sided smoothing is near-zero-phase → dissolves the over-smoothing-lag worry. Fusion stays causal; calibration gets nicer odometry.
- **Impl note (Slice 10)**: `ofc::TwistSmoother` (`smoother.hpp`/`.cpp`) — augmented CV model `x = [twist(6); accel(6)]`, `F = [[I, dt·I],[0,I]]`, white-accel `Q = q·[[dt³/3, dt²/2],[dt²/2, dt]]⊗I` (`q = kf_process_noise`), measurement `H=[I,0]`, `R = r·I` (`r` fixed 1.0; only `q/r` shapes smoothing). Forward KF + **backward RTS pass** over a depth-`L+1` window emits the lag-`L` smoothed twist (true fixed-lag two-sided, verified **zero-phase**: emitted err <0.1× the causal-lag floor; variance ↓~0.3×). Wired into the calibration feed at the deeper frontier (gated by `per_sensor_kf`; OFF ⇒ byte-identical). Calibration peaks ~4× sharper (extrinsic_confidence 0.038→0.156) with no estimate bias. **Open**: a single shared smoother uses the MAX `q` over enabled sources (not per-source `q`/`r`); the refined RTS covariance is computed/exposed but NOT yet wired into the calibrator vote weight (the deeper path still feeds the raw Σ-confidence) — D18's "refined Σ" promise is half-wired; ~7.5 MB fixed footprint (compile-time caps); the lag is a step count via the nominal tick rate (off-cadence ⇒ effective time-lag ≠ `calib_lag_s`).

## D19 — Config system → **validated POD struct + adapter parsing**
- **Chosen**: core takes one validated config struct, preallocates once at init; YAML/JSON/ROS loaders are adapters that *build* the struct.
- **Rejected**: built-in file config (parser + heap + IO in core); compile-time config (recompile to retune); hybrid compile-sizes (capacities need recompile).
- **Requirement**: a **config-reference doc** documenting every knob (user-stressed).

## D20 — Scale calibration → **straight-regime magnitude-ratio histogram**
- **Chosen**: per-source translation scale `s_i` = magnitude ratio vs the pinned reference, recovered in the **straight** regime (pure translation → zero lever-arm confound), voted into a scale histogram, fed back **pre-median**. Per-source on/off; metric sources fixed at 1.
- **Rejected**: joint-with-lever-arm in Phase 2 (nonlinear, confounds); none (wrong for wheel-scale drift / mono-VO); single global scale (per-source by nature).
- **Why**: real for automotive (tire pressure/wear drifts wheel-odom scale); slots into existing regime + machinery.

## D21 — ESKF state → **pose + twist, SO(3)×ℝ³ error, calib external**
- **Chosen**: state = `SE(3)` pose + `ℝ⁶` twist; decoupled `SO(3)×ℝ³` pose error (matches split-metric median) + `ℝ⁶` twist; dense 12×12; calibration params **not** in state.
- **Rejected**: pose-only coupled (cruder twist Σ/tip); IEKF-coupled (declined path); augmented-with-calib (contradicts separate-histogram calibration, bloats filter).
- **Impl note (Slice 2)**: error-state ordering is `[trans; rot]` (pose 0–5, twist 6–11). Predict on a delta uses right-error transport `F = blkdiag(Ad(delta⁻¹), 0)` (twist re-read each step as `log(delta)/dt`, so its prior error doesn't carry), `P ← F P Fᵀ + blkdiag(Q, Q/dt²)`. Adaptive `Q = q_scale·spread² · I₆ + diag(q_floor)`. Per source, the delta is **scale-corrected then frame-aligned**: `B_corr = {B.R, B.t / prior_scale}`, then `A = X ∘ B_corr ∘ X⁻¹` with `X = prior_extrinsic` (sensor→base). Fusion applying `prior_scale` is no longer deferred (pre-median scale *calibration* still is). Accepted Slice-2 approximations: zero pose↔twist cross-covariance; `sigma_confidence` unit-mixing (until Slice 9 reliability EMA).
- **Time-offset sign convention (sim/oracle, Slice-5 contract)**: **positive `prior_time_offset_s` shifts the sampled window later** — a source's reported motion for `[t0,t1]` corresponds to true motion over `[t0+off, t1+off]` (source clock ahead of base). Slice-5 time-sync must invert this sign.
- **Impl note (Slice 14, covariance tangent)**: the "decoupled `SO(3)×ℝ³`" wording describes the error *ordering* (`[trans; rot]`) and the **median's** split metric — NOT the covariance. The ESKF propagates `P` with the **full SE(3) adjoint** `F = Ad(delta⁻¹)` (the `[t]ₓR` coupling block), so the dense 12×12 covariance lives in the **full coupled SE(3) tangent**; NEES/consistency must use the full-SE(3) `se3::log` error to match it (verified in Slice-14 review). **Init-P fix (`70c7d38`)**: the filter now inits `P = 0` at the gauge-anchored first fuse and lets the first `predict()` establish the one-window covariance `blkdiag(q_pose, q_pose/dt²)` (was `I₁₂`), cutting the pessimism from ~46× (NEES ≈ 0.13) to ~17× (NEES ≈ 0.35). The residual is the predict-only `Ad(delta⁻¹)` translation-block inflation over accumulated motion (no correction to shrink it; rotation block well-calibrated) — strict no-ref consistency needs a distance-aware covariance model / no-ref correction. See DESIGN §10.

## D22 — Absolute refs → **generic measurement plugin + optional per-source bias states**
- **Chosen**: one interface `evaluate(state) → (residual, H, R)` + stamp, Mahalanobis-gated; position/pose/orientation agnostic; **loop-closure excluded** (needs past states ⇒ smoother). **Plus optional per-source bias states** (config per-source).
- **Why the bias states** (user's GPS/IMU experience): the classic "like a charm" GPS/INS drift removal comes from **online IMU bias estimation via GPS cross-covariance** — the plain plugin (no bias states) corrects fused pose but *can't recalibrate a raw IMU*. Bias is a fast nuisance state observable only via an absolute ref at the fusion level → belongs in the filter (genuinely different from the slow geometric extrinsics in histograms). Off by default; enable for a raw-IMU source.
- **Impl note (Slice 11, correction path)**: the **plugin + Mahalanobis-gated ESKF update half** of D22 shipped (`Eskf::update`: `S=HPHᵀ+R`, NIS gate, gain via one LDLT, Joseph covariance, right-error full-SE(3) injection; `Estimator::add_correction` wired into `step()` after predict; `Result::CorrectionDiag`; `validate()` `mahalanobis_chi2>0`). Sim `SyntheticAbsoluteRef` position fix → drift removed (0.58→0.20 m); gross outlier rejected (NIS ~3e5); NIS now computable (~2.4 vs DOF 3). The gate WAS a single scalar regardless of measurement DOF — **now per-`n`** (Slice 11b, `e8491dd`): `Eskf::chi2_gate(base, n)` scales the n=3-tuned `mahalanobis_chi2` base by the χ²-quantile ratio `q[n]/q[3]` (const `kChi2Q95[7]`, 0.95 confidence) so every DOF `n∈1..6` gates at the same confidence (n=3 unchanged → dim=3 fixes behaviorally identical; n=6 ≈ 1.61×base); `update()`/`update_aug()` keep their raw-threshold signatures, the estimator does the per-`n` scaling at the call site. Bias states + GPS adapter were deferred to Slice 11b (see below).
- **Impl note (Slice 11b Option A)**: chosen + shipped (`48ece29` + `f47b29a`). The architecture problem: D22's GPS/INS bias-estimation assumes the biased sensor *drives the predict*, but here the **median of N sources** drives it — so the pose↔bias cross-covariance doesn't form classically. **Option A** handles the regime where it IS exact: a SINGLE `bias_states` source driving the predict alone augments the ESKF to **18-DOF** `[pose;twist;bias(6 body-twist rate)]`; predict de-biases `Δ_db = Δ∘exp(-b·dt)` and builds the cross-cov via `J_pb = -dt·I₆` (the exact leading term — reviewer-verified the full Jacobian is `-dt·Ad(exp(b·dt))`, omitted part O(dt²)); the absolute-ref update injects `δb` through the cross-cov (Joseph, 18-DOF). New knob `bias_process_noise`; single-bias guard (`>1 → InvalidConfig`); `predict_aug_frozen` keeps the filter consistent out-of-regime; default-OFF byte-identical. Sim: planted bias recovered, drift 16 m → 0.06 m, no-ref observability self-test. The **per-`n` χ² Mahalanobis gate** is also DONE (`e8491dd`, see the Slice-11 impl note above), and the **concrete GPS adapter** is DONE (`4bce8d1`): `ofc_adapters::GpsCorrection` (relaxed edge) — geodetic GPS → WGS-84 ECEF → ENU about a datum → odom via a configurable `odom_from_enu` SE3; dim=3 position correction with a lever arm (`H=[R|−R·[l]×|0]`); end-to-end drift removal 0.133→0.0075 m + outlier rejection through the dim=3 per-`n` gate. Surfaced caveat: the GPS Kalman gain couples to `q_floor` (zero adaptive-q spread → `q_pose`→`q_floor`; too-small floor starves the gain) — documented in CONFIG §3. **Still DEFERRED (11b residual)**: **Option B** only (median-coupled multi-source bias via the per-source median weight in the augmented `F` — general N-source case, higher risk; design in `ISSUES.md` Slice-11b DESIGN NOTE).

## D23 — Startup → **lifecycle state machine, degrade-don't-block** (+ user: persistence)
- **Chosen**: `INIT → WARMUP → DEGRADED → NOMINAL`; emit best-available (reference-only) output ASAP, auto-upgrade as calibration converges; readiness + confidence exposed; graceful downgrade on fault; gauge anchored at first fused tick.
- **Impl note (Slice 3)**: the ladder is a single per-step recompute from the participating-source count `n` (no high-water-mark — downgrade falls out for free). `INIT` set at `init()` (observable before the first step); pre-fuse (`t1≤q0` or `n==0`) → `WARMUP`; first successful fuse latches `ever_fused` and gives `NOMINAL` iff `n ≥ min_sources_warn` (new `Config` knob, default 3; `validate()` enforces lower-bound `≥1` only — `> max_sources` is a legitimate "never NOMINAL" config) else `DEGRADED`; once fused, a non-fusing step is `DEGRADED`+`NotReady` and keeps the last good frontier. A coarse `Result::readiness` scalar (Init 0.0 / Warmup 0.25 / Degraded 0.6 / Nominal 1.0) is published alongside `phase` via a single `publish_phase()` sink so the two can never drift. NOMINAL is source-count-driven; calibration-convergence coupling is only indirect (via `ReferenceOnly` cold-start participation) — a deliberate Slice-3 scope boundary (per-DOF convergence is already exposed in `CalibSnapshot`).
- **User addition — warm-restart persistence**: config on/off, periodic; persist **calibration state**; **double-buffer ping-pong** (two files + sequence counter, version-tagged + checksummed) so a crash mid-write keeps the last good state; invalidate on setup change via manual delete **+ auto config-hash guard**; serialize/deserialize in core, file IO in adapter.
- **Impl note (Slice 12)**: core `Estimator::serialize`/`deserialize` into a caller fixed buffer (`persistence.hpp`: "OFCP" magic + `format_version` 1 + FNV-1a-64 `config_hash` + payload + FNV-1a-32 checksum; explicit-little-endian, padding-free, IEEE-754-bit-exact doubles; new Status `CorruptData`/`VersionMismatch`). Validation order magic→version→exact-length→checksum→config-hash (no OOB read before the checksum) + a restored-extrinsic orthonormality guard. `config_hash` covers rig shape + per-sensor priors/flags + all histogram configs + commit/gate thresholds + `phase2_strategy`/`calib_lag_s` (runtime knobs tick_rate/fusion_delay/output are deliberately excluded). **Histogram bins are NOT persisted** (deviates from the literal §7 "histogram bins"): deserialize restores committed values + flags + reliability + lifecycle and **re-anchors** the calibrators; hysteresis (+ an `offset_restored` latch) holds restored-committed DOFs through the empty-histogram refill → near-NOMINAL (warm ~0.001 m vs cold ~0.256 m). The **double-buffer ping-pong file IO is relaxed-edge** (test-side here; the production adapter is Slice 13).

## D24 — Validation → **layered, sim-GT backbone**
- **Chosen**: unit (per block) + **sim rig with known ground truth** (only place calibration *correctness* is checkable) + **observability self-tests** (each DOF converges only in its regime → guards the spine) + **NEES/NIS** consistency (a Σ-publishing filter must prove calibration of its own covariance) + recorded-data **golden regression** (determinism → byte-stable).
- **Impl note (Slice 14)**: NEES Monte-Carlo (6-DOF pose, full-SE(3) tangent, ensemble over seeds) + golden regression (deterministic noise-free committed snapshot + byte-identical replay incl. the calib snapshot) implemented in `tests/test_validation.cpp`. **NIS deferred to Slice 11** (no `ICorrection` updates → no innovation yet). **Finding**: the published Σ is **pessimistic ~46×** (ensemble NEES ≈ 0.13 ≪ DOF 6) — init `P=I₁₂` + predict-only Ad inflation; needs a smaller init-P and/or a Slice-11 correction step (not Q-tunable). The CONFIG "tuned"-placeholder sweep remains open → Slice 14 stays `[~]`.

## D25 — Histogram sub-bin readout → **opt-in 3-bin centroid** (Slice 16)
- **Problem (2026-06-10 investigation)**: the committed real-data yaw calib read ~0.23° off truth (vs the 0.1° user target). A discriminator experiment (KAIST `calib_test`, injected yaw) ruled out vote weighting (one ≈ combo to 3e-5 rad) and commit dynamics; falsification probes (truth planted at a bin center vs a quarter-bin offset) pinned the cause: **`mode()`'s parabolic sub-bin interpolation is systematically biased toward the peak-bin center** (a split point-mass at fraction f reads `0.5f/(2−3f)` bins, ~70–80% pull; worst ~0.3–0.45° at the so3 bin width 1.79°). The same pull showed simultaneously in yaw, scale, and time-offset of the same run; the historical "0.10°" was luck (10° ≈ a bin center). Commit looked worse only because the re-anchor moves the operating point to an uncontrolled sub-bin phase.
- **Chosen**: `HistogramConfig::subbin_centroid` (default **false**) — when true (and `subbin`), `mode()` returns the mass-weighted centroid over peak±1 (`c_p + (h_{p+1}−h_{p−1})/Σm · w`), which is **exact** for a vote-split point mass at any sub-bin position; ≤~1% truncation bias at our peak concentrations. Wrap-aware (unwrapped neighbor centers, folded back); one-sided at a non-circular boundary (no fall-back-to-center cliff). Loader `[global] subbin_centroid` fans out to all five calib histograms. Flag joins the persistence config-hash → **all pre-Slice-16 persisted blobs cold-start** (intentional).
- **Rejected**: changing the default (re-validates the whole tuned trust apparatus); zoom-on-commit re-anchor (finer bins post-commit — more machinery, range-vs-noise coupling; kept as fallback, not needed).
- **Validated (KAIST urban07 calib_test, centroid ON)**: yaw error sub-bin-phase-independent at +0.015° across all three probes; **committed** yaw 0.0102° (was 0.229°), committed scale err ~8e-5 (was 1.8e-3), time-offset err 2.6 µs (was 2.2 ms), committed lever [0.5007, 0.1975] vs [0.5, 0.2] (~2.5 mm). Sim/default byte-identical; cov-cal NEES guard untouched. Commits `c237bfb` + `00978c8`; design doc `SLICE16_CENTROID_SUBBIN_READOUT.md`.

## D26 — Turn-regime rotation extrinsic → **axis-correspondence Wahba (rot3d)** (Slice 17)
- **Problem**: EuRoC exposed the rotation extrinsic as unrecoverable on 3D/drone motion (conf 0) — Phase-1's direction method needs the straight regime, which a drone lacks; the wrong rotation also corrupts the lever hand-eye rows at ~|t|·θ.
- **Chosen**: extend Phase-2 with the rotation-axis correspondence of the hand-eye identity: `a = R_X·b` per turn-gated windowed delta (`a = log(R_A)`, `b = log(R_B)`); decay-aged per-slot Wahba accumulator `Mw = Σ w·a bᵀ` + axis Gram `BBw`; running Kabsch solve gated on **two distinct axes** (`λ_mid ≥ kAxisPairMin(1e-2)·λ_max` of `BBw`); residual `δφ = log(R̂·R_bpᵀ)` voted into 3 so(3) channels (reuse `so3_hist`), contractive rising-edge re-anchor; committed rot3d supersedes the ext/roll publish and joins the median under `ReferenceOnly`. Once the gate is open the running R̂ also drives the lever rows' `R_X` (replacing frozen `R_yp·Rx(roll)` there), and the rot3d rising edge resets the lever accumulator (stale-rotation rows). `Config::rot3d_enabled` default **false** (everything byte-identical off); persistence **format v2** (committed flag persisted; pre-17 blobs cold-start). Prototype (`tools/proto_rot_handeye.py`) validated the math first: exact on clean EuRoC windows; KAIST yaw-only gives BBw rank 1 with the missing yaw EXACTLY the about-axis blind DOF — complementary to Phase-1 by construction (the spine).
- **Rejected**: partial rank-1 subspace voting on ground (double-estimates pitch vs Phase-1 — feedback fight for zero new observability); replacing `best_roll` (stays as the rank-1 turn estimator); feeding `set_yaw_pitch` from rot3d (same fight); batch-SVD-only readout (loses histogram robustness + commit machinery).
- **Validated (EuRoC V1_01, rot3d+centroid+one)**: rotation extrinsic err **1.6e-7 rad (9.1e-6°), COMMITTED** on real drone motion; lever µm-exact with unit-scale sources ([0.299988, −0.200001, 0.150000] vs [0.3, −0.2, 0.15]). KAIST planar with the flag ON: byte-identical results (gate honest). **Scale-coupling finding**: with an unrecovered source scale (1.08) the lever reads ~3.5 cm off along the motion axis — the row uses raw `t_B`; scale stays unobservable on a drone (no straight windows). The row is linear in `(t_X, 1/s)` → a turn-regime JOINT lever+scale solve is the natural follow-up (would close the last calib DOF on 3D motion). Commits `f1eee59`+`2d22c28`+`f013320`; design `SLICE17_TURN_REGIME_ROTATION_EXTRINSIC.md`; review `reviews/slice-17-findings.md`.

## D27 — Turn-regime scale → **joint lever+scale hand-eye LS** (Slice 17b)
- **Problem**: per-source scale was straight-regime-only (Phase-1 magnitude ratio) → unobservable on a drone; an unrecovered scale corrupted the Slice-17 lever via the raw-magnitude `t_B` in the rows (~3.5 cm at scale 1.08).
- **Chosen**: the hand-eye translation identity is LINEAR in `[t_X; κ=1/s_res]`: `(R_A−I)t_X − κ(R_X t_B) = −t_A` (t_B arrives prior-de-scaled → κ is the RESIDUAL scale, Phase-1's convention). Phase-2's lever LS grows 3→4 unknowns behind `Config::joint_lever_scale` (default **false**, byte-identical off): 4×4 running ridge (centered `[t_prior; 1]`), per-axis votes gated on the **marginal (Schur) information** (`kJointMarginMin=1e-2` of the raw diagonal — a lever/κ mixed near-null direction under ≤2 distinct turn excitations must yield conf 0, not a confident wrong commit), lever axes → existing xyz hists, `s_res=1/κ̂` → a new per-slot scale2 histogram (`scale_hist` shape) with an **out-of-range skip guard** (a clamped edge-bin vote from a transient would look concentrated, commit, and poison `prior_scale` irreversibly — fold is a value, not a vote; skipped votes are counted in `scale2_skipped(id)`, so out-of-range ≠ no-excitation diagnostically; the deliberate cost: a TRUE residual outside the `scale_hist` range never commits on a turn-only rig — divergent from Phase-1's iterating clamp, documented). Estimator: `scale2_committed` via the same gate; a rising edge of EITHER scale path folds `prior_scale ×= s_res` and resets BOTH scale hists (+ the joint accumulator — a fold moves the de-scale epoch; sequential edges → no double-fold); snapshot scale = prior × the more-confident residual (tie → Phase-1). Persistence **format v3**.
- **Rejected**: sharing Phase-1's scale histogram (couples calibrator internals); replacing Phase-1 scale (straight votes stay the denser ground-regime source — the two cross-check); always-on (silently changes every lever number); edge-clamped out-of-range votes (the poisoning ratchet, mutation-pinned).
- **Validated**: prototype `tools/proto_lever_scale.py` (EuRoC: lever 1.8e-8 mm + scale 2.7e-12 vs the 3-unknown control's 88 mm; KAIST planar: κ well-conditioned, z the lone null). Real data, full recipe (`rot3d+joint_lever_scale+subbin_centroid+vote_weight=one`): **EuRoC scale 1.07962 vs 1.08 COMMITTED from the turn regime; lever sub-mm ([0.299868,−0.199952,0.14995]) with the scale UNKNOWN**; KAIST turn-path scale 1.10008 agrees with the straight path, lever ~10× tighter. ALL calibration DOFs (rotation extrinsic, lever, scale, time-offset) now meet the user precision targets on both ground and 3D motion. Commits `4138c3d`+`0ea764a`; design `SLICE17B_JOINT_LEVER_SCALE.md`; review `reviews/slice-17b-findings.md`.

## D28 — Multi-source bias states → **exact median-influence Ω_i, landed opt-in; real-data GPS channel CANNOT feed it** (Slice 18)
- **Motivation**: the fused heading on real data tracks the MIDDLE yaw-drifter (urban12: fused 5–30° wander vs FOG's 4.6° full-drive) — Slice-9 reliability can't help (the middle source has the lowest residual by construction). De-bias each source pre-median (11b Option B).
- **Chosen**: the deferred Option B, with one major correction: **spike 5a's scalar coupling `−dt·ω_i·Ad(X_i)` is FALSIFIED** against the true interior median (it was FD-verified against the old D3 pinning median, an input passthrough; the real median's input sensitivity carries a radial projector no scalar expresses — 38–98% error). The exact first-order influence (implicit function theorem at the Weiszfeld fixed point, FD-pinned worst 1.5%, `tests/test_multi_bias.cpp`): `Ω_i = M⁻¹u_iP_i`, `P_i = I−ξ_i(Wξ_i)ᵀ/d_i²`, `ΣΩ_i = I`; `J_{pose,b_i} = blkdiag(R_mᵀ,I)·Ω_i·blkdiag(R_{A_i},I)·(−dt·Ad(X_i))`. State `[pose;twist;bias_1..k]`, k≤4 (Mat36); single de-bias site (fusion AND calibrators see the same de-biased deltas); `multi_bias_enabled` default false (Option A byte-identical); per-DOF `bias_process_noise` Vec6 (**0 pins a DOF**), `multi_bias_cov0` knob. Sim: planted-bias separation, coast drift 4.4×/14× better, NEES never-overconfident.
- **Hardening that outlived the slice**: out-of-range calibration-vote SKIP guards (so3/xyz/rot3d, the 17b scale2 pattern + skip counters) — on real data, wandering bias rotated the de-biased deltas until calib votes edge-clamped and COMMITTED at ±0.984375 (the boundary-bin center), destroying urban12 (tail 1.99→2716 m). Boundary mass can never commit now. Active regardless of the bias flag.
- **Real-data verdict (the honest boundary, instrumented `bias_wz` harness column)**: at sim-scale priors a single GPS fix kicks yaw bias to +9870°/h (junk sink → divergence); at honest physical priors (σ≈206°/h) the filter learns ±3°/h of the true −75°/h in 41 min, `bias_observable` 0.000, run ≡ flag-off to 5 decimals. Structural: position-only GPS observes yaw-rate bias only through the rotation gain rows that **C4 (D-15b) deliberately suppresses**; the cheap channel (source-vs-source rate differences) is discarded by the median by design, and source-as-measurement is D4's rejected double-count. **Ship opt-in (sim-valid regimes: rich corrections, no C4 conflict); the urban heading prize needs a different lever (per-DOF consensus weighting = a D3-level revisit, future decision).** urban07/12 no-regression at physical scale confirmed. Commits `def0cee`/`2dc6678`/`67b0242`/`57fc874`/`9756d46`; `SLICE18_MEDIAN_COUPLED_BIAS.md`; review `reviews/slice-18-findings.md`.

## D29 — Fusion weighting → **per-channel split median** (D3 amendment, Slice 19; USER-approved 2026-06-12)
- **Problem**: one scalar weight per source cannot express per-channel quality — the fused heading tracked the MIDDLE yaw-drifter (urban12: 5–30° band, 61° spike, vs the FOG source's 4.6°); Slice-18 proved de-biasing can't fix it (observability boundary, D28) — weighting can.
- **Chosen (user forks)**: (1) split the median per channel — independent Weiszfeld solves on SO(3) (geodesic) and ℝ³, each with the FULL D3 safeguard set (off-vertex init, Vardi-Zhang, ε-1/d, caps, n≤2 closed forms, high-weight-outlier guard per channel); per-channel spreads (rad/m, no λ unit-mixing) → per-channel Q. (2) Weight policy layer (a) only: `SensorConfig::rot_weight_prior` (declare the gyro grade — a datasheet fact); layers (b) scatter-reliability and (c) GPS-course drift monitor deferred. **Layer (b) SHIPPED** (Slice 19b, `51b86bf` — see the D17 Slice-19b impl note). **Layer (c) SHIPPED** (Slice 19c, `770f6b2`+`e7bba55` — see the impl note below). (3) Cross-channel outlier veto default-ON: `d_i > 3 × max(LOO spread, floor 0.01 rad/0.02 m)` in one channel scales the source ×0.1 in the other (one bounded re-solve; floors prevent the near-coincident hair-trigger; full-set RMS was dead code — `d/spread ≤ √n`). `Config::split_median` default **false** (coupled path byte-identical; flip after soak); **`q_scale_split` = 3.0** (own calibrated value — the coupled 0.7 is overconfident under split, NEES 21.7: the mixed spread had padded both channels' Q; sweep extended past the spec brace with the band intact, ratified). Ω_i under split = block-diagonal per-channel IFT (FD-pinned 1.1% worst, veto-scaled weights).
- **Rejected**: per-source 6×6 metric weights inside one coupled solve (non-standard median theory, unverifiable); inter-source statistics as the weight policy (the middle trap is information-theoretic at n=3 drifters); source-as-measurement (D4's double-count).
- **Validated (KAIST, recommended urban config + FOG rot prior 10)**: urban12 local rot p50 0.0133→**0.0041 rad (0.23°)**, full-drive rms rot 5.4° ≈ the FOG floor — the heading prize delivered; urban07 better everywhere; urban17 neutral. One urban12 worst-window regression 120→211 m logged. Commits `6ebfcc5`+`6c23894`; `SLICE19_SPLIT_CHANNEL_MEDIAN.md`; review `reviews/slice-19-findings.md`.
- **Impl note (Slice 19b, layer b — per-channel scatter reliability)**: see the D17 Slice-19b impl note (per-channel residual EMAs under split → `rel_rot`/`rel_trans`; coupled path byte-identical). `51b86bf`.
- **Impl note (Slice 19c, layer c — GPS-course heading-drift monitor)**: a strict-core `HeadingMonitor` (own `heading_monitor.hpp/.cpp`) ranks per-source heading drift against GPS course-over-ground and boosts the heading-grade source's ROTATION-channel weight, auto-discovering the FOG with no config (`w_rot = clamp(w_base·rel_rot)·rot_weight_prior·monitor_boost`). **Ranking, not absolute bias** — it sidesteps the D28 observability boundary (position-only GPS can't observe per-source yaw bias absolutely, but source-vs-course drift RATES are directly comparable). Mechanics (ported + FD-faithful to the validated prototype `tools/proto_heading_monitor.py`): anchor gating (straight-at-speed fixes only) → telescoping residual pairs that BRIDGE turns and are NEVER hard-dropped (a drop injects a permanent offset — the load-bearing prototype lesson) → cross-source median residual split (rate + clamped-excess event channels) → per-GPS-contiguous-segment slope estimator (60-s **median** blocks, baseline-weighted median of pairwise slopes, bounded 256 reservoir) → `boost = clip(boost_max·max(min_score,floor)/max(score_i,floor), 1, boost_max)` with the `kScoreFloor = 2.42e-5 rad/s (5°/h)` GPS-noise floor, ≥2-scored abstain, slow update + rank-flip hysteresis (≥2 consecutive). `Config::heading_monitor` default **false** (OFF byte-identical BOTH paths AND in the abstain state — both pinned); `heading_monitor_boost_max` default 10; monitor-without-split → `validate()` InvalidConfig; both knobs config-hashed; monitor state NOT persisted; diagnostics in `SourceHealth.{heading_score,heading_boost,heading_scored}`. **Decided in review**: block reduction is MEDIAN not mean (event-channel design proves short blocks carry outliers; the first cut shipped mean, fixed); diagnostics home = `SourceHealth` (over `CalibSnapshot`). **Validated (KAIST, urban recipe, `rot_weight_prior` REMOVED)**: FOG (src1) auto-discovered on ALL THREE drives (boost 10 vs 1; scores rank-correct vs GT/prototype), reproducing the manual recipe with zero config; absolute <10° heading met everywhere; urban17 neutral; the COST is discovery latency (early-drive abstain window not yet FOG-preferred → urban12 local rot p50 0.23°→0.49° on the GPS-hostile ~1300 s canyon start, urban07 mild) ⇒ layer (c) is the tool when the heading-grade source is UNKNOWN/drifting; the config prior (layer a) wins when it is known. Follow-up: warm-start the boost from `rot_weight_prior` to erase the latency. Commits `770f6b2`+`e7bba55`; `SLICE19C_HEADING_MONITOR.md`; review `reviews/slice-19c-findings.md`.

## D30 — Translation-only source lever → **pin rotation to prior, run the existing lever LS** (Slice 20)
- **Problem**: a velocity/Doppler-only source (radar; per-step `R_B = I`) gets lever conf 0 AND commits a confidently-WRONG rotation extrinsic (a real footgun feeding bad rotation back to fusion). Root cause (investigated): the lever row `(R_A−I)t_X = R_X t_B − t_A` is already correct and already gates on the CONSENSUS turn — the only break is `R_X`, which falls back to Phase-1's `R_yp` for a heading-blind source, and Phase-1 fits garbage yaw/pitch from the absent/noise rotation (probe: a front radar committed `rx = −0.984 rad` at conf 0.95).
- **Chosen**: `SensorConfig::translation_only` (bool, default false). When set, PIN the rotation extrinsic to the prior — skip the Phase-1 direction (yaw/pitch) vote, skip rot3d accumulation/vote, skip the per-window roll — so the lever row uses `R_X = prior` (clean); keep scale calibration; the rotation snapshot stays = prior with `extrinsic_committed = false` (trusted, not recovered). All behind the flag → non-flagged sources byte-identical (the Phase-1 direction-vote refactor preserves the scale-vote-drop on reverse/near-π samples — pinned). `validate()` requires an orthonormal prior rotation for such a source; flag joins the config-hash (pre-20 blobs cold-start). Along-track guard = the EXISTING histogram-concentration gate already withholds the noisy axis (measured conf→0; no new residual gate — simpler-is-better).
- **Rejected**: a whole new lever estimator (the existing LS is already correct — only `R_X` was poisoned); a per-source LS-residual gate (the concentration gate already withholds; unneeded); co-estimating `R_X` from the heading-blind source (the prototype's wrong-R_X probe = ∞ degradation — `R_X` MUST come from the trusted prior).
- **Prototype** (`tools/proto_radar_lever.py`, `8df5de8`): the linear estimator is exact (synthetic ~1e-13 cm); on real nuScenes radar the LATERAL lever recovers to ~5-25 cm with a trusted `R_X`, the ALONG-TRACK lever is a sensor SNR wall (~2 m, not averageable), z unobservable on planar.
- **Real-data verdict (the honest boundary)**: the footgun fix is CONFIRMED on real radar (all 5 radars hold `extr_rot = prior`, conf 0, no garbage) — the primary, verified win. But the radar LEVER does NOT commit on real data (conf 0) and the readout overshoots the lateral — because (1) `translation_confidence = min(cx,cy,cz)` with z unobservable on planar pins the min at 0, and (2) the production per-window/consensus-base estimator is materially noisier than the prototype's idealized GT-base BATCH LS. The proto over-promised by using GT-base + batch; the full path revealed the gap (the recurring validate-on-the-real-path lesson). Committed radar-lever recovery needs `min`-over-OBSERVED-axes confidence + lower per-window radar noise (follow-ups). Commit `629d379`; `SLICE20_TRANSLATION_ONLY_LEVER.md`; review `reviews/slice-20-review.md`.

---

## Leaf defaults (locked)
Confidence-combine = **sum** (configurable); Phase-2 histograms roll = circular S¹, xyz = 3×1-D; commit on peak-concentration ≥ τ ∧ votes ≥ N with hysteresis; Weiszfeld bounded iters + ε-regularized `1/d` + split-metric weight `λ`; gate thresholds documented; reverse-fold into the consensus/prior hemisphere before voting.

## The spine, in one sentence
Each calibration DOF is observable in exactly one motion regime, and the math hands you the gate for free (the lever-arm least-squares goes singular precisely when there is no rotation). Every gate, vote, and weight derives from that.
